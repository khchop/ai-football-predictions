---
phase: 73-archive-system-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/llm/index.ts
  - src/lib/db/queries.ts
  - src/lib/db/queries/stats.ts
autonomous: true
must_haves:
  truths:
    - "Archived models are NOT queried by the prediction pipeline workers"
    - "getActiveModelCount() returns count excluding archived models"
    - "getOverallStats().activeModels excludes archived models"
    - "Leaderboard queries can optionally include/exclude archived models"
    - "Leaderboard query results include archived status for each model"
  artifacts:
    - path: "src/lib/llm/index.ts"
      provides: "Pipeline exclusion of archived models + active model count"
      exports: ["getActiveProviders", "getActiveModelCount"]
    - path: "src/lib/db/queries.ts"
      provides: "getOverallStats excludes archived, getTopModelsByCompetition excludes archived by default"
      contains: "archived"
    - path: "src/lib/db/queries/stats.ts"
      provides: "Leaderboard queries with includeArchived parameter and archived field in results"
      exports: ["getLeaderboard", "getLeaderboardWithTrends"]
  key_links:
    - from: "src/lib/llm/index.ts"
      to: "src/lib/db/schema.ts"
      via: "drizzle query on models.archived"
      pattern: "models\\.archived"
    - from: "src/lib/db/queries/stats.ts"
      to: "src/lib/db/schema.ts"
      via: "archived filter in WHERE clause"
      pattern: "archived"
---

<objective>
Exclude archived models from prediction pipeline, active model counts, and leaderboard queries (with opt-in inclusion).

Purpose: Archived models should no longer participate in predictions or appear in counts/rankings by default, but leaderboard queries must support an opt-in flag to show them (for the UI toggle in Plan 02).

Output: Updated query functions across 3 files that respect the `archived` column added in Phase 72.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-model-configuration-archive-schema/72-01-SUMMARY.md
@src/lib/db/schema.ts
@src/lib/llm/index.ts
@src/lib/db/queries.ts
@src/lib/db/queries/stats.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude archived models from pipeline and counts</name>
  <files>src/lib/llm/index.ts, src/lib/db/queries.ts</files>
  <action>
  In `src/lib/llm/index.ts`:

  1. Update `getActiveProviders()` to also exclude archived models:
     - Import `models` table and `archived` column from db schema (already imported: `getDb, models`)
     - After getting `disabledIds`, also query for archived model IDs:
       ```
       const archivedIds = await getArchivedModelIds();
       ```
     - Filter providers: `!disabledIds.has(p.id) && !archivedIds.has(p.id)`
     - Add a new function `getArchivedModelIds()` similar to `getAutoDisabledModelIds()` in queries.ts, OR import it from queries.ts

  2. Update `getActiveModelCount()` to exclude archived models:
     - Change the WHERE clause from `eq(models.active, true)` to `and(eq(models.active, true), eq(models.archived, false))`
     - Import `and` from drizzle-orm (already imported: `eq, sql`)

  In `src/lib/db/queries.ts`:

  3. Add `getArchivedModelIds()` function next to `getAutoDisabledModelIds()` (around line 992):
     ```typescript
     export async function getArchivedModelIds(): Promise<Set<string>> {
       const db = getDb();
       const results = await db
         .select({ id: models.id })
         .from(models)
         .where(eq(models.archived, true));
       return new Set(results.map(r => r.id));
     }
     ```

  4. Update `getOverallStats()` (line 1454):
     - Change the active_models subquery from `WHERE active = true` to `WHERE active = true AND archived = false`

  5. Update `getTopModelsByCompetition()` (line 280):
     - Add `eq(models.archived, false)` to the WHERE clause alongside `eq(models.active, true)`

  6. Update `shouldSkipModelDueToHealth()` (line 1003) to also check archived:
     - `return model.autoDisabled === true || model.archived === true;`
  </action>
  <verify>
  - `npx tsc --noEmit --project tsconfig.json 2>&1 | head -20` shows no type errors in modified files
  - `grep -n 'archived' src/lib/llm/index.ts` shows archived filtering in getActiveProviders and getActiveModelCount
  - `grep -n 'archived' src/lib/db/queries.ts` shows archived filtering in getOverallStats, getTopModelsByCompetition, and getArchivedModelIds
  </verify>
  <done>
  - getActiveProviders() excludes both auto-disabled AND archived models
  - getActiveModelCount() returns count of models where active=true AND archived=false
  - getOverallStats().activeModels excludes archived models
  - getTopModelsByCompetition() excludes archived models by default
  - getArchivedModelIds() function exists and returns Set of archived model IDs
  - shouldSkipModelDueToHealth() returns true for archived models
  </done>
</task>

<task type="auto">
  <name>Task 2: Add includeArchived parameter to leaderboard queries</name>
  <files>src/lib/db/queries/stats.ts</files>
  <action>
  In `src/lib/db/queries/stats.ts`:

  1. Update `LeaderboardEntry` interface (line 52) to include archived status:
     - Add `archived: boolean;` field

  2. Update `LeaderboardEntryWithTrend` interface to inherit the archived field (it extends LeaderboardEntry, so this is automatic).

  3. Update `LeaderboardFilters` interface (line 251) to add:
     - `includeArchived?: boolean;` // Default false when not specified

  4. Update `getLeaderboard()` function (line 269):
     - Add `models.archived` to the `selectFields` object:
       ```typescript
       archived: models.archived,
       ```
     - In the WHERE conditions builder (line 285), when `includeArchived` is NOT true, add:
       ```typescript
       if (!filters?.includeArchived) {
         whereConditions.push(eq(models.archived, false));
       }
       ```
       This applies to BOTH the filtered and unfiltered paths.
     - In the unfiltered path (line 372), change the simple `eq(models.active, true)` to also include the archived filter when includeArchived is not set.
     - In the return mapping (line 379), add `archived: r.archived ?? false` to the returned objects.
     - NOTE: The `archived` field needs to be included in the `groupBy(models.id)` -- but since models.id is the primary key, grouping by it already covers all model columns in PostgreSQL, so no change needed there.

  5. Update `getLeaderboardWithTrends()` function (line 400):
     - Apply the same `includeArchived` filter logic in `buildBaseConditions()`:
       ```typescript
       if (!filters?.includeArchived) {
         conditions.push(eq(models.archived, false));
       }
       ```
     - Add `archived: models.archived` to `selectFields` (line 471).
     - Add `archived: r.archived ?? false` to the return mapping (line 565).

  6. Import the `models` table if not already imported. Check line 2 -- it already imports `models` from `@/lib/db`.

  7. In both functions, make sure the `selectFields` includes `archived` and the return type maps it through.
  </action>
  <verify>
  - `npx tsc --noEmit --project tsconfig.json 2>&1 | head -20` shows no type errors
  - `grep -n 'includeArchived' src/lib/db/queries/stats.ts` shows the parameter in filters and usage in query conditions
  - `grep -n 'archived' src/lib/db/queries/stats.ts` shows archived field in select, return mapping, and filter logic
  </verify>
  <done>
  - LeaderboardEntry interface has `archived: boolean` field
  - LeaderboardFilters interface has `includeArchived?: boolean` field
  - getLeaderboard() excludes archived models by default, includes them when includeArchived=true
  - getLeaderboardWithTrends() excludes archived models by default, includes them when includeArchived=true
  - Both functions return `archived` field in results for UI rendering
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` passes with zero errors
2. All leaderboard queries default to excluding archived models
3. Pipeline (getActiveProviders) excludes archived models
4. Active model count excludes archived models
5. Leaderboard queries accept includeArchived flag for opt-in
6. Leaderboard results include archived boolean field
</verification>

<success_criteria>
- ARCH-02: Archived models excluded from prediction pipeline (getActiveProviders filters them)
- ARCH-03: Archived models excluded from getActiveModelCount() dynamic count
- Backend support for ARCH-04/05/06: Leaderboard queries support includeArchived parameter
- Backend support for ARCH-07/08: Same queries power team/competition leaderboards via filters
</success_criteria>

<output>
After completion, create `.planning/phases/73-archive-system-integration/73-01-SUMMARY.md`
</output>
