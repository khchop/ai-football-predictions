---
phase: 72-model-configuration-archive-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - migrations/006_add_archived_column.sql
  - src/lib/llm/providers/openrouter.ts
autonomous: true

must_haves:
  truths:
    - "Models table schema defines an `archived` boolean column with default false"
    - "Migration SQL file exists and adds `archived` column to models table"
    - "14 new OpenRouterProvider instances are defined with correct OpenRouter model IDs"
    - "MiniMax M2.1 provider instance is updated with current pricing"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "archived column on models table"
      contains: "archived.*boolean.*default.*false"
    - path: "migrations/006_add_archived_column.sql"
      provides: "Database migration for archived column"
      contains: "ALTER TABLE models ADD COLUMN archived"
    - path: "src/lib/llm/providers/openrouter.ts"
      provides: "14 new model provider instances + 1 updated"
      contains: "glm-5-or|deepseek-v3.2-or|deepseek-r1-0528-or"
  key_links:
    - from: "src/lib/db/schema.ts"
      to: "migrations/006_add_archived_column.sql"
      via: "schema column matches migration DDL"
      pattern: "archived.*boolean"
    - from: "src/lib/llm/providers/openrouter.ts"
      to: "OpenRouter API"
      via: "model field matches OpenRouter model ID"
      pattern: "new OpenRouterProvider"
---

<objective>
Add database archive infrastructure and define 14 new OpenRouter model providers.

Purpose: Lay the foundation for the 20-model configuration by (1) adding the `archived` column to the models schema for Phase 73 archive integration, and (2) defining all new model provider instances with correct OpenRouter IDs, pricing, and prompt configs.

Output: Schema with archived column, migration SQL, and 14 new + 1 updated OpenRouterProvider instances in openrouter.ts.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/db/schema.ts
@src/lib/llm/providers/openrouter.ts
@src/lib/llm/prompt-variants.ts
@src/lib/llm/response-handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add archived column to models schema and create migration</name>
  <files>src/lib/db/schema.ts, migrations/006_add_archived_column.sql</files>
  <action>
1. In `src/lib/db/schema.ts`, add an `archived` boolean column to the `models` pgTable definition. Place it after the `autoDisabled` field (line ~79):
   ```
   archived: boolean('archived').default(false),
   ```

2. Add an index on the `archived` column in the table's index definitions (the function after the column defs):
   ```
   index('idx_models_archived').on(table.archived),
   ```

3. Create `migrations/006_add_archived_column.sql` with:
   ```sql
   -- Add archived column to models table for model lifecycle management
   -- Archived models retain historical data but are excluded from active operations
   ALTER TABLE models ADD COLUMN IF NOT EXISTS archived BOOLEAN DEFAULT false;
   CREATE INDEX IF NOT EXISTS idx_models_archived ON models (archived);
   ```

This is ARCH-01. The column defaults to false so existing models remain non-archived.
  </action>
  <verify>
- `grep -n "archived" src/lib/db/schema.ts` shows the column definition
- `cat migrations/006_add_archived_column.sql` shows valid SQL
- `npx tsc --noEmit --pretty false 2>&1 | head -20` compiles without type errors on schema.ts
  </verify>
  <done>Models schema has `archived` boolean column (default false) with index, and migration SQL file exists.</done>
</task>

<task type="auto">
  <name>Task 2: Define 14 new model providers and update MiniMax pricing</name>
  <files>src/lib/llm/providers/openrouter.ts</files>
  <action>
Add 14 new OpenRouterProvider instances AFTER the existing model definitions (after line ~325, before the OPENROUTER_PROVIDERS array). Follow the exact same constructor pattern as existing models.

**Important conventions:**
- Provider ID format: `{model-slug}-or` (e.g., `glm-5-or`)
- Provider name: always `'openrouter'`
- Model field: exact OpenRouter API model ID (e.g., `z-ai/glm-5`)
- Display name format: `'{Model Name} (OpenRouter)'`

**14 new model definitions to add:**

1. **GLM-5** (premium, reasoning model - needs ENGLISH_ENFORCED + EXTRACT_JSON like GLM-4.7):
   - id: `'glm-5-or'`
   - model: `'z-ai/glm-5'`
   - displayName: `'GLM-5 (OpenRouter)'`
   - tier: `'premium'`
   - pricing: `{ promptPer1M: 0.80, completionPer1M: 2.56 }`
   - isPremium: `true`
   - promptConfig: `{ promptVariant: PromptVariant.ENGLISH_ENFORCED, responseHandler: ResponseHandler.EXTRACT_JSON, timeoutMs: 120000, supportsJsonMode: false, maxTokensSingle: 1000, maxTokensBatch: 1500 }`

2. **DeepSeek V3.2** (budget - needs JSON_STRICT like V3.1):
   - id: `'deepseek-v3.2-or'`
   - model: `'deepseek/deepseek-v3.2'`
   - displayName: `'DeepSeek V3.2 (OpenRouter)'`
   - tier: `'budget'`
   - pricing: `{ promptPer1M: 0.25, completionPer1M: 0.38 }`
   - isPremium: `false`
   - promptConfig: `{ maxTokensSingle: 500, maxTokensBatch: 1000 }`

3. **DeepSeek R1-0528** (premium, reasoning - needs THINKING_STRIPPED + STRIP_THINKING_TAGS like R1):
   - id: `'deepseek-r1-0528-or'`
   - model: `'deepseek/deepseek-r1-0528'`
   - displayName: `'DeepSeek R1-0528 (OpenRouter)'`
   - tier: `'premium'`
   - pricing: `{ promptPer1M: 0.40, completionPer1M: 1.75 }`
   - isPremium: `true`
   - promptConfig: `{ promptVariant: PromptVariant.THINKING_STRIPPED, responseHandler: ResponseHandler.STRIP_THINKING_TAGS, timeoutMs: 120000 }`

4. **Devstral Small** (budget, code-focused):
   - id: `'devstral-small-or'`
   - model: `'mistralai/devstral-small'`
   - displayName: `'Devstral Small (OpenRouter)'`
   - tier: `'budget'`
   - pricing: `{ promptPer1M: 0.10, completionPer1M: 0.30 }`
   - isPremium: `false`
   - promptConfig: `{}`

5. **Qwen3 30B A3B** (ultra-budget, MoE):
   - id: `'qwen3-30b-a3b-or'`
   - model: `'qwen/qwen3-30b-a3b'`
   - displayName: `'Qwen3 30B A3B (OpenRouter)'`
   - tier: `'ultra-budget'`
   - pricing: `{ promptPer1M: 0.06, completionPer1M: 0.22 }`
   - isPremium: `false`
   - promptConfig: `{}`

6. **GPT-OSS-20B** (ultra-budget - needs JSON_STRICT + EXTRACT_JSON like GPT-OSS-120B):
   - id: `'gpt-oss-20b-or'`
   - model: `'openai/gpt-oss-20b'`
   - displayName: `'GPT-OSS 20B (OpenRouter)'`
   - tier: `'ultra-budget'`
   - pricing: `{ promptPer1M: 0.03, completionPer1M: 0.14 }`
   - isPremium: `false`
   - promptConfig: `{ promptVariant: PromptVariant.JSON_STRICT, responseHandler: ResponseHandler.EXTRACT_JSON, timeoutMs: 45000, supportsJsonMode: false, maxTokensSingle: 1000, maxTokensBatch: 1500 }`

7. **Step 3.5 Flash** (budget, MoE):
   - id: `'step-3.5-flash-or'`
   - model: `'stepfun/step-3.5-flash'`
   - displayName: `'Step 3.5 Flash (OpenRouter)'`
   - tier: `'budget'`
   - pricing: `{ promptPer1M: 0.10, completionPer1M: 0.30 }`
   - isPremium: `false`
   - promptConfig: `{}`

8. **Mistral Small 3.2 24B** (ultra-budget, replaces Mistral Small 3):
   - id: `'mistral-small-3.2-24b-or'`
   - model: `'mistralai/mistral-small-3.2-24b-instruct'`
   - displayName: `'Mistral Small 3.2 24B (OpenRouter)'`
   - tier: `'ultra-budget'`
   - pricing: `{ promptPer1M: 0.06, completionPer1M: 0.18 }`
   - isPremium: `false`
   - promptConfig: `{}`

9. **Gemma 3 27B** (ultra-budget):
   - id: `'gemma-3-27b-or'`
   - model: `'google/gemma-3-27b-it'`
   - displayName: `'Gemma 3 27B (OpenRouter)'`
   - tier: `'ultra-budget'`
   - pricing: `{ promptPer1M: 0.04, completionPer1M: 0.15 }`
   - isPremium: `false`
   - promptConfig: `{}`

10. **Trinity Large Preview** (free tier, MoE):
    - id: `'trinity-large-preview-or'`
    - model: `'arcee-ai/trinity-large-preview:free'`
    - displayName: `'Trinity Large Preview (OpenRouter)'`
    - tier: `'free'`
    - pricing: `{ promptPer1M: 0.00, completionPer1M: 0.00 }`
    - isPremium: `false`
    - promptConfig: `{}`

11. **Phi-4** (ultra-budget, 14B):
    - id: `'phi-4-or'`
    - model: `'microsoft/phi-4'`
    - displayName: `'Phi-4 (OpenRouter)'`
    - tier: `'ultra-budget'`
    - pricing: `{ promptPer1M: 0.06, completionPer1M: 0.14 }`
    - isPremium: `false`
    - promptConfig: `{}`

12. **Llama 4 Scout** (ultra-budget, MoE):
    - id: `'llama-4-scout-or'`
    - model: `'meta-llama/llama-4-scout-17b-16e-instruct'`
    - displayName: `'Llama 4 Scout (OpenRouter)'`
    - tier: `'ultra-budget'`
    - pricing: `{ promptPer1M: 0.08, completionPer1M: 0.30 }`
    - isPremium: `false`
    - promptConfig: `{}`

13. **Gemma 3 12B** (ultra-budget):
    - id: `'gemma-3-12b-or'`
    - model: `'google/gemma-3-12b-it'`
    - displayName: `'Gemma 3 12B (OpenRouter)'`
    - tier: `'ultra-budget'`
    - pricing: `{ promptPer1M: 0.03, completionPer1M: 0.10 }`
    - isPremium: `false`
    - promptConfig: `{}`

14. **SKIP Nemotron 3 Nano 30B A3B** - NOT available on OpenRouter (model returns 404). Instead, keep `qwen3-235b-thinking-or` as the 20th model to maintain the target count.

**Also update MiniMax M2.1** (existing provider `MiniMaxM21_OR`):
- Update pricing from `{ promptPer1M: 0.35, completionPer1M: 0.70 }` to `{ promptPer1M: 0.27, completionPer1M: 0.95 }`
- Keep all other config the same

Do NOT modify the OPENROUTER_PROVIDERS array yet (Plan 02 handles that).
Do NOT modify MODEL_PROVIDER_ROUTES yet (Plan 02 handles that).
  </action>
  <verify>
- Count new `OpenRouterProvider` constructor calls: should be 14 new (not counting MiniMax update)
- `npx tsc --noEmit --pretty false 2>&1 | head -20` compiles without type errors
- Each new provider's `model` field matches a valid OpenRouter API model ID
  </verify>
  <done>14 new OpenRouterProvider instances defined. MiniMax M2.1 pricing updated. All use correct OpenRouter model IDs and appropriate prompt configs.</done>
</task>

</tasks>

<verification>
- Schema: `grep "archived" src/lib/db/schema.ts` returns the boolean column definition
- Migration: `cat migrations/006_add_archived_column.sql` contains valid ALTER TABLE
- New models: `grep -c "new OpenRouterProvider" src/lib/llm/providers/openrouter.ts` shows 32 total (18 existing + 14 new)
- TypeScript: `npx tsc --noEmit` passes
</verification>

<success_criteria>
1. ARCH-01 complete: models schema has `archived` boolean column with default false and migration file exists
2. MDL-02 partially complete: 14 new model instances defined (Nemotron 30B unavailable), MiniMax updated
3. All new providers have correct OpenRouter model IDs verified against API
4. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/72-model-configuration-archive-schema/72-01-SUMMARY.md`
</output>
