---
phase: 57-category-fixes-fallbacks-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/llm/index.ts
  - scripts/diagnostic/validate-coverage.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "All 13 Synthetic models evaluated for Together AI fallback eligibility"
    - "Existing 3 fallback mappings validated as correct and complete"
    - "10 Synthetic-exclusive models documented with skip rationale"
    - "Coverage validation script confirms 95%+ theoretical coverage"
  artifacts:
    - path: "src/lib/llm/index.ts"
      provides: "Enhanced MODEL_FALLBACKS documentation with exhaustive exclusion rationale"
      contains: "MODEL_FALLBACKS"
    - path: "scripts/diagnostic/validate-coverage.ts"
      provides: "Offline coverage validation script"
      exports: ["validateCoverage"]
    - path: "package.json"
      provides: "validate:coverage npm script"
      contains: "validate:coverage"
  key_links:
    - from: "scripts/diagnostic/validate-coverage.ts"
      to: "src/lib/llm/index.ts"
      via: "imports ALL_PROVIDERS, MODEL_FALLBACKS"
      pattern: "import.*ALL_PROVIDERS.*MODEL_FALLBACKS"
---

<objective>
Audit all 13 Synthetic models for Together AI fallback eligibility, confirm the 3 existing
mappings are the only valid ones, document why each of the 10 exclusive models cannot have
fallbacks, and create an offline coverage validation script that verifies model configuration
completeness.

Purpose: FIX-05 requires expanding fallback chains where equivalents exist. Research shows
no new expansions are possible (10 models are Synthetic-exclusive). This plan codifies that
finding into the codebase and creates tooling to validate coverage going forward.

Output: Enhanced MODEL_FALLBACKS documentation, coverage validation script, npm script
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-category-fixes-fallbacks-validation/57-RESEARCH.md
@src/lib/llm/index.ts
@src/lib/llm/providers/synthetic.ts
@src/lib/llm/providers/together.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit fallback mappings and enhance MODEL_FALLBACKS documentation</name>
  <files>src/lib/llm/index.ts</files>
  <action>
    Audit the MODEL_FALLBACKS mapping in src/lib/llm/index.ts. The existing 3 mappings are
    correct and represent the ONLY valid Synthetic-to-Together fallbacks:
    - deepseek-r1-0528-syn -> deepseek-r1 (same model family)
    - kimi-k2-thinking-syn -> kimi-k2-instruct (same base, different tuning)
    - kimi-k2.5-syn -> kimi-k2-instruct (same family fallback)

    Enhance the comment block below MODEL_FALLBACKS to document each of the 10
    Synthetic-exclusive models that CANNOT have fallbacks, with the specific reason:

    1. deepseek-v3-0324-syn: DeepSeek V3 0324 not available on Together AI
    2. deepseek-v3.1-terminus-syn: DeepSeek V3.1 Terminus not available on Together AI
    3. deepseek-v3.2-syn: DeepSeek V3.2 not available on Together AI
    4. minimax-m2-syn: MiniMax M2 not available on Together AI
    5. minimax-m2.1-syn: MiniMax M2.1 not available on Together AI
    6. glm-4.6-syn: GLM 4.6 not available on Together AI
    7. glm-4.7-syn: GLM 4.7 not available on Together AI
    8. qwen3-coder-480b-syn: Qwen3 Coder 480B not available on Together AI
    9. gpt-oss-120b-syn: Together only has GPT-OSS 20B (different model size, not equivalent)
    10. qwen3-235b-thinking-syn: Together has Qwen3 235B Instruct but NOT Thinking variant;
        cross-variant fallback risks different output style (same rationale as not falling
        back GLM to Qwen)

    Format the comment block as a structured table showing:
    Model ID | Reason No Fallback | Mitigation (prompt/handler config)

    Also add a comment header "Phase 57 Audit: 3/13 mappable, 10/13 exclusive" above the
    existing Note comment, replacing the current comment with the exhaustive list.

    Do NOT add any new fallback mappings. Do NOT change any executable code. This is
    documentation-only to codify the Phase 57 research findings.
  </action>
  <verify>
    grep -c "deepseek-v3-0324-syn\|minimax-m2-syn\|glm-4.6-syn\|qwen3-coder-480b-syn\|gpt-oss-120b-syn" src/lib/llm/index.ts
    should return 5+ (confirming each exclusive model is documented).
    Existing 3 mappings must still be present and unchanged.
    npm run build should succeed (no code changes, only comments).
  </verify>
  <done>
    MODEL_FALLBACKS in index.ts documents all 13 Synthetic models with explicit
    fallback status (3 mapped, 10 excluded with reason). Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create offline coverage validation script</name>
  <files>scripts/diagnostic/validate-coverage.ts, package.json</files>
  <action>
    Create scripts/diagnostic/validate-coverage.ts that performs offline (no API keys needed)
    validation of model coverage and configuration completeness. This script:

    1. Imports ALL_PROVIDERS from src/lib/llm and MODEL_FALLBACKS from src/lib/llm/index.ts
    2. Imports PromptVariant and ResponseHandler enums from src/lib/llm/prompt-variants or
       wherever they are defined (check imports in synthetic.ts for exact paths)
    3. For each of the 42 providers, checks:
       a. Has a non-empty id, name, and model
       b. Has pricing configured (promptPer1M > 0, completionPer1M > 0)
       c. For Synthetic providers: checks if fallback mapping exists in MODEL_FALLBACKS
       d. For models with special configs: validates promptVariant and responseHandler are set
    4. Produces a coverage report to stdout:
       - Total models: 42 (29 Together + 13 Synthetic)
       - Fallback coverage: 3/13 Synthetic models have Together AI fallbacks
       - Special config coverage: Lists models with non-default promptVariant/responseHandler
       - Uncovered models: Lists Synthetic models without fallback AND without special config
         (these are potential risk models)
    5. Exits 0 if all validations pass, exits 1 if any model is misconfigured
    6. Does NOT make any API calls - this is purely configuration validation

    Add npm script: "validate:coverage": "npx tsx scripts/diagnostic/validate-coverage.ts"

    Pattern: Follow existing scripts/diagnostic/*.ts patterns (same import style, same
    output formatting as generate-report.ts).

    Key logic for "theoretical coverage":
    - A model is "covered" if ANY of these is true:
      (a) It's a Together AI model (reliable baseline)
      (b) It has a fallback mapping to Together AI
      (c) It has special promptVariant + responseHandler configured
    - "Uncovered" = Synthetic model with no fallback AND default config (BASE + DEFAULT)
    - Current state: Only DeepSeek V3 0324, DeepSeek V3.1 Terminus, MiniMax M2, MiniMax M2.1,
      and Qwen3 Coder 480B use default config without fallbacks
    - Report theoretical coverage percentage: (covered / total) * 100
  </action>
  <verify>
    npx tsx scripts/diagnostic/validate-coverage.ts should run without errors and produce
    a coverage report showing 42 total models, 3 fallback mappings, and list of uncovered
    models. Exit code 0 if no misconfiguration found.
    npm run validate:coverage should work as alias.
  </verify>
  <done>
    Coverage validation script exists, runs offline, reports model coverage status for all
    42 models, and npm script validate:coverage is available. Script identifies any Synthetic
    models that are both fallback-less AND use default config as potential risk models.
  </done>
</task>

</tasks>

<verification>
1. src/lib/llm/index.ts has enhanced documentation for all 13 Synthetic models
2. scripts/diagnostic/validate-coverage.ts exists and runs without API keys
3. npm run validate:coverage produces coverage report with 42 models
4. npm run build passes (no code regressions)
5. Existing 3 fallback mappings unchanged
</verification>

<success_criteria>
- MODEL_FALLBACKS documents all 13 Synthetic models (3 mapped, 10 excluded with rationale)
- Coverage validation script runs offline and identifies risk models
- Build passes with no regressions
- FIX-05 addressed: All possible fallback expansions evaluated and documented
</success_criteria>

<output>
After completion, create `.planning/phases/57-category-fixes-fallbacks-validation/57-01-SUMMARY.md`
</output>
