---
phase: 55-category-fixes-timeouts-tags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/diagnostic/analyze-timeouts.ts
  - src/lib/llm/providers/together.ts
  - src/lib/llm/providers/synthetic.ts
  - src/__tests__/fixtures/test-data.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "Reasoning models have timeouts >= 90s based on P95 + 20% safety margin"
    - "DeepSeek R1 (Together) timeout increased from 60s to at least 90s"
    - "DeepSeek R1 0528 (Synthetic) timeout increased from 60s to at least 90s"
    - "Kimi K2 Thinking (Synthetic) timeout increased from 60s to at least 90s"
    - "Qwen3 235B Thinking (Synthetic) timeout unchanged or increased (never decreased)"
    - "REASONING_MODEL_TIMEOUT in test-data.ts matches the maximum reasoning model timeout"
  artifacts:
    - path: "scripts/diagnostic/analyze-timeouts.ts"
      provides: "Timeout analysis script that reads diagnostic raw responses and recommends timeouts"
    - path: "src/lib/llm/providers/together.ts"
      provides: "Updated DeepSeek R1 timeout"
      contains: "timeoutMs:"
    - path: "src/lib/llm/providers/synthetic.ts"
      provides: "Updated reasoning model timeouts"
      contains: "timeoutMs:"
    - path: "src/__tests__/fixtures/test-data.ts"
      provides: "Updated REASONING_MODEL_TIMEOUT constant"
      contains: "REASONING_MODEL_TIMEOUT"
  key_links:
    - from: "scripts/diagnostic/analyze-timeouts.ts"
      to: "src/__tests__/diagnostic-results/raw-responses/"
      via: "reads raw response JSON files"
      pattern: "raw-responses"
    - from: "src/lib/llm/providers/base.ts"
      to: "PromptConfig.timeoutMs"
      via: "production timeout selection"
      pattern: "promptConfig.*timeoutMs"
---

<objective>
Tune per-model timeouts for reasoning models based on diagnostic P95 latency data.

Purpose: Reasoning models (DeepSeek R1, Qwen3-235B-Thinking, Kimi K2-Thinking) fail with timeout errors because their chain-of-thought processing takes longer than the configured 60s limit. Data-driven timeout tuning using P95 + 20% safety margin eliminates timeout failures without over-provisioning.

Output: Updated timeout values in provider configurations and a reusable analysis script.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/llm/providers/together.ts
@src/lib/llm/providers/synthetic.ts
@src/lib/llm/providers/base.ts
@src/__tests__/fixtures/test-data.ts
@.planning/phases/55-category-fixes-timeouts-tags/55-RESEARCH.md
@.planning/phases/54-diagnostic-infrastructure/54-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create timeout analysis script and analyze diagnostic data</name>
  <files>scripts/diagnostic/analyze-timeouts.ts, package.json</files>
  <action>
Create `scripts/diagnostic/analyze-timeouts.ts` that:

1. Reads all JSON files from `src/__tests__/diagnostic-results/raw-responses/` (if directory exists)
2. For each model, extracts `durationMs` from successful results
3. Calculates P50, P95, P99, and max latency per model
4. Applies P95 * 1.2 (20% safety margin), rounded up to nearest 5s
5. Compares recommended timeout against current PromptConfig.timeoutMs
6. Prints a table showing: modelId, samples, P50, P95, current timeout, recommended timeout, action (INCREASE/KEEP)
7. Focuses on reasoning models: deepseek-r1, deepseek-r1-0528-syn, kimi-k2-thinking-syn, qwen3-235b-thinking-syn

If no diagnostic raw response files exist yet (user hasn't run `npm run diagnose`), the script should:
- Print a warning: "No diagnostic data found. Run `npm run diagnose` first for data-driven tuning."
- Print default recommendations based on industry data:
  - deepseek-r1: 120000ms (Azure reports 2-minute limits needed)
  - deepseek-r1-0528-syn: 120000ms (same model, different host)
  - kimi-k2-thinking-syn: 90000ms (conservative default)
  - qwen3-235b-thinking-syn: 120000ms (large thinking model)

Add npm script to package.json:
```json
"analyze:timeouts": "npx tsx scripts/diagnostic/analyze-timeouts.ts"
```

Place it near the existing `diagnose` script.

Import DiagnosticResult type from `./categorize-failure` for type safety.
Use `import { readdir, readFile } from 'fs/promises'` and `import path from 'path'`.
  </action>
  <verify>
Run `npx tsx scripts/diagnostic/analyze-timeouts.ts` - should print default recommendations (no diagnostic data yet) without errors.
Verify `npm run analyze:timeouts` script exists in package.json.
  </verify>
  <done>Script prints timeout recommendations table for all 4 reasoning models. Script handles both "with data" and "without data" paths gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Update reasoning model timeouts in provider configurations</name>
  <files>src/lib/llm/providers/together.ts, src/lib/llm/providers/synthetic.ts, src/__tests__/fixtures/test-data.ts</files>
  <action>
Update PromptConfig.timeoutMs for reasoning models. Use conservative defaults (since diagnostic data likely doesn't exist yet). These can be refined after running diagnostics.

CRITICAL RULES:
- ONLY increase timeouts, NEVER decrease (per research Pitfall 5)
- Production timeout is PromptConfig.timeoutMs, NOT REASONING_MODEL_IDS (per research Pitfall 3)

**In `src/lib/llm/providers/together.ts`:**
- DeepSeekR1Provider: change `timeoutMs: 60000` to `timeoutMs: 120000`
  - Rationale: Azure reports 2-minute limit needed for DeepSeek R1, P95 latency commonly 60-90s

**In `src/lib/llm/providers/synthetic.ts`:**
- DeepSeekR1_0528_SynProvider: change `timeoutMs: 60000` to `timeoutMs: 120000`
  - Rationale: Same model as Together variant, same timeout needed
- KimiK2Thinking_SynProvider: change `timeoutMs: 60000` to `timeoutMs: 90000`
  - Rationale: Conservative increase, smaller reasoning overhead than DeepSeek R1
- Qwen3_235BThinking_SynProvider: keep `timeoutMs: 90000` (already 90s) OR increase to `timeoutMs: 120000`
  - Decision: increase to 120000ms (large 235B thinking model warrants same buffer as DeepSeek R1)

**In `src/__tests__/fixtures/test-data.ts`:**
- Update `REASONING_MODEL_TIMEOUT` from `90000` to `120000` to match the maximum reasoning model timeout
- Add a comment: `// Matches max PromptConfig.timeoutMs for reasoning models (deepseek-r1, qwen3-thinking)`

Do NOT modify:
- Any non-reasoning model timeouts
- Any promptVariant or responseHandler values (that's Plan 55-02)
- The REASONING_MODEL_IDS set (already correct)
- The getModelTimeout() function logic (already correct)
  </action>
  <verify>
1. `grep -n "timeoutMs" src/lib/llm/providers/together.ts` shows 120000 for DeepSeek R1
2. `grep -n "timeoutMs" src/lib/llm/providers/synthetic.ts` shows 120000 for DeepSeek R1 0528, 90000 for Kimi K2 Thinking, 120000 for Qwen3 235B Thinking
3. `grep -n "REASONING_MODEL_TIMEOUT" src/__tests__/fixtures/test-data.ts` shows 120000
4. `npx tsc --noEmit` compiles without errors
  </verify>
  <done>All 4 reasoning models have increased timeouts in their PromptConfig. REASONING_MODEL_TIMEOUT in test-data.ts matches the maximum (120s). No non-reasoning model timeouts changed. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/diagnostic/analyze-timeouts.ts` runs without errors and prints recommendations
2. `grep timeoutMs src/lib/llm/providers/together.ts` shows 120000 for DeepSeek R1
3. `grep timeoutMs src/lib/llm/providers/synthetic.ts` shows updated reasoning model timeouts
4. `grep REASONING_MODEL_TIMEOUT src/__tests__/fixtures/test-data.ts` shows 120000
5. `npx tsc --noEmit` passes (or `npm run build` if available)
6. No non-reasoning model configurations changed
</verification>

<success_criteria>
- All 4 reasoning models have PromptConfig.timeoutMs >= 90000ms
- DeepSeek R1 variants have 120000ms (2 minutes) timeout
- REASONING_MODEL_TIMEOUT updated to 120000ms
- Timeout analysis script exists and runs without errors
- No non-reasoning model timeouts were modified
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/55-category-fixes-timeouts-tags/55-01-SUMMARY.md`
</output>
