---
phase: 55-category-fixes-timeouts-tags
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - src/lib/llm/providers/together.ts
  - src/lib/llm/providers/synthetic.ts
autonomous: true

must_haves:
  truths:
    - "All reasoning models have BOTH promptVariant: THINKING_STRIPPED AND responseHandler: STRIP_THINKING_TAGS"
    - "No reasoning model is missing either the prompt variant or the response handler"
    - "Regression tests confirm working models are unaffected by Phase 55 changes"
    - "Thinking tag leakage is addressed by both prevention (prompt) and cleanup (handler)"
  artifacts:
    - path: "src/lib/llm/providers/together.ts"
      provides: "DeepSeek R1 with correct thinking tag handlers"
      contains: "STRIP_THINKING_TAGS"
    - path: "src/lib/llm/providers/synthetic.ts"
      provides: "All 3 Synthetic reasoning models with correct thinking tag handlers"
      contains: "STRIP_THINKING_TAGS"
  key_links:
    - from: "src/lib/llm/providers/together.ts"
      to: "src/lib/llm/response-handlers.ts"
      via: "ResponseHandler.STRIP_THINKING_TAGS import"
      pattern: "ResponseHandler\\.STRIP_THINKING_TAGS"
    - from: "src/lib/llm/providers/synthetic.ts"
      to: "src/lib/llm/prompt-variants.ts"
      via: "PromptVariant.THINKING_STRIPPED import"
      pattern: "PromptVariant\\.THINKING_STRIPPED"
---

<objective>
Audit and validate thinking tag handler configuration for all reasoning models, then run regression tests to confirm Phase 55 changes don't break working models.

Purpose: Thinking tags (`<think>`, `<thinking>`, `<reasoning>`) in model output break JSON parsing. The belt-and-suspenders approach (prompt variant prevents generation + response handler strips tags) must be consistently applied to ALL reasoning models. Regression testing validates that timeout changes from Plan 55-01 and handler verification don't introduce regressions.

Output: Verified handler configuration for all reasoning models and confirmed regression test results.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/llm/providers/together.ts
@src/lib/llm/providers/synthetic.ts
@src/lib/llm/response-handlers.ts
@src/lib/llm/prompt-variants.ts
@.planning/phases/55-category-fixes-timeouts-tags/55-RESEARCH.md
@.planning/phases/55-category-fixes-timeouts-tags/55-01-SUMMARY.md
@.planning/phases/53-regression-protection/53-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix reasoning model handler configuration</name>
  <files>src/lib/llm/providers/together.ts, src/lib/llm/providers/synthetic.ts</files>
  <action>
Audit ALL 42 models (29 Together + 13 Synthetic) to verify reasoning model handler configuration is complete.

**Reasoning model identification criteria:**
A model is a reasoning model if its ID or name contains: `r1`, `thinking`, or `reasoning` (case-insensitive).

**Required configuration for every reasoning model (belt-and-suspenders):**
1. `promptVariant: PromptVariant.THINKING_STRIPPED` (prevention: instructs model NOT to generate thinking tags)
2. `responseHandler: ResponseHandler.STRIP_THINKING_TAGS` (cleanup: strips tags if model ignores instruction)

**Known reasoning models to audit:**

Together AI (1):
- `deepseek-r1` — Verify has BOTH promptVariant: THINKING_STRIPPED AND responseHandler: STRIP_THINKING_TAGS

Synthetic (3):
- `deepseek-r1-0528-syn` — Verify has BOTH
- `kimi-k2-thinking-syn` — Verify has BOTH
- `qwen3-235b-thinking-syn` — Verify has BOTH

**Additional scan:**
Check ALL other models for any that might be reasoning models but are missing handlers. Specific candidates to verify are NOT reasoning models (and should NOT get thinking handlers):
- `kimi-k2-0905` (Instruct variant, NOT thinking)
- `kimi-k2-instruct` (Instruct variant, NOT thinking)
- `qwen3-235b-instruct` (Instruct variant, NOT thinking)
- `kimi-k2.5-syn` (Non-thinking variant)

If any reasoning model is missing either handler, add both. Based on current codebase inspection, all 4 reasoning models already have correct configuration. If this is confirmed, document the audit result in the summary but no code changes are needed for this task.

If any model needs a fix, apply it. Do NOT change promptVariant or responseHandler for any non-reasoning model.
  </action>
  <verify>
1. `grep -A5 "THINKING_STRIPPED" src/lib/llm/providers/together.ts` shows DeepSeek R1 has both variant and handler
2. `grep -A5 "THINKING_STRIPPED" src/lib/llm/providers/synthetic.ts` shows all 3 Synthetic reasoning models have both variant and handler
3. No non-reasoning model has THINKING_STRIPPED or STRIP_THINKING_TAGS configured
  </verify>
  <done>All 4 reasoning models confirmed to have BOTH promptVariant: THINKING_STRIPPED AND responseHandler: STRIP_THINKING_TAGS. No non-reasoning model has thinking handlers. Audit complete.</done>
</task>

<task type="auto">
  <name>Task 2: Run regression tests to confirm no regressions from Phase 55 changes</name>
  <files></files>
  <action>
Run the regression test suite (Phase 53) to validate that Phase 55 changes (timeout increases from Plan 01 + handler verification from Task 1) don't break anything.

**Step 1: Run offline regression tests**
```bash
npm run test:regression
```

Expected: All tests pass. These are offline tests using golden fixtures, so they validate that parser and schema infrastructure is intact.

**Step 2: Verify TypeScript compilation**
```bash
npx tsc --noEmit
```

Expected: No type errors. Timeout changes are just number value changes, handler changes (if any) use existing enums.

**Step 3: Document results**
Record the regression test output and compilation status. If any tests fail:
- Identify which model/test failed
- Determine if the failure is pre-existing (from before Phase 55) or a regression
- Fix only if it's a regression caused by Phase 55 changes

**Step 4: Verify the final state of all reasoning models**
Print a summary showing each reasoning model's final configuration:
```
Model ID                    | Timeout  | PromptVariant          | ResponseHandler
deepseek-r1                 | 120000ms | THINKING_STRIPPED      | STRIP_THINKING_TAGS
deepseek-r1-0528-syn        | 120000ms | THINKING_STRIPPED      | STRIP_THINKING_TAGS
kimi-k2-thinking-syn        | 90000ms  | THINKING_STRIPPED      | STRIP_THINKING_TAGS
qwen3-235b-thinking-syn     | 120000ms | THINKING_STRIPPED      | STRIP_THINKING_TAGS
```

This summary proves all Phase 55 success criteria are met.
  </action>
  <verify>
1. `npm run test:regression` passes with 0 failures
2. `npx tsc --noEmit` passes with 0 errors
3. All 4 reasoning models have increased timeouts and correct handlers
  </verify>
  <done>Regression tests pass. TypeScript compiles cleanly. All 4 reasoning models have correct timeout AND handler configuration. No working models broken by Phase 55 changes.</done>
</task>

</tasks>

<verification>
1. All 4 reasoning models have both THINKING_STRIPPED prompt variant and STRIP_THINKING_TAGS response handler
2. No non-reasoning model has thinking tag handlers
3. `npm run test:regression` passes with 0 failures
4. `npx tsc --noEmit` passes with 0 errors
5. Final configuration summary shows all reasoning models with correct timeout + handler setup
</verification>

<success_criteria>
- Audit confirms all reasoning models have belt-and-suspenders thinking tag handling
- No reasoning model is missing either the prompt variant or the response handler
- Regression tests pass (working models unaffected by Phase 55 changes)
- TypeScript compiles without errors
- Phase 55 success criteria 1-5 from ROADMAP.md are all met
</success_criteria>

<output>
After completion, create `.planning/phases/55-category-fixes-timeouts-tags/55-02-SUMMARY.md`
</output>
