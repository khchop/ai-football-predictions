---
phase: 69-ui-components-match-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/queries/team-stats.ts
  - src/components/team/team-model-leaderboard.tsx
  - src/components/team/team-leaderboard-filter.tsx
  - src/components/team/team-form-indicator.tsx
  - src/components/team/team-stats-overview.tsx
  - src/app/teams/[slug]/page.tsx
autonomous: true
must_haves:
  truths:
    - "User can see per-club model leaderboard ranked by average points"
    - "User can filter club leaderboard by time period (all-time, season, monthly, weekly)"
    - "User can view club statistics with W/D/L record, goals scored/conceded, and averages"
    - "User can see a visual form indicator (W/D/L timeline) for the club's last 5 matches"
  artifacts:
    - path: "src/components/team/team-model-leaderboard.tsx"
      provides: "Client component rendering team-scoped model leaderboard with TanStack Table"
      exports: ["TeamModelLeaderboard"]
    - path: "src/components/team/team-leaderboard-filter.tsx"
      provides: "Client component for time period filter (all, season, monthly, weekly)"
      exports: ["TeamLeaderboardFilter"]
    - path: "src/components/team/team-form-indicator.tsx"
      provides: "Client component rendering W/D/L colored badges timeline"
      exports: ["TeamFormIndicator"]
    - path: "src/components/team/team-stats-overview.tsx"
      provides: "Server component rendering enhanced stats cards with home/away splits"
      exports: ["TeamStatsOverview"]
    - path: "src/lib/db/queries/team-stats.ts"
      provides: "getTeamModelLeaderboard query wrapper"
      exports: ["getTeamModelLeaderboard"]
  key_links:
    - from: "src/app/teams/[slug]/page.tsx"
      to: "src/lib/db/queries/team-stats.ts"
      via: "getTeamModelLeaderboard() call"
      pattern: "getTeamModelLeaderboard"
    - from: "src/app/teams/[slug]/page.tsx"
      to: "src/components/team/team-model-leaderboard.tsx"
      via: "JSX rendering with leaderboard data prop"
      pattern: "TeamModelLeaderboard"
    - from: "src/components/team/team-leaderboard-filter.tsx"
      to: "URL search params"
      via: "useRouter().push() to update ?timePeriod="
      pattern: "timePeriod"
---

<objective>
Build the team model leaderboard, time period filtering, enhanced stats display, and form indicator for team pages.

Purpose: Fulfills STAT-01 (per-club leaderboard), STAT-02 (time period filtering), STAT-03 (enhanced club stats), and MTCH-03 (form indicator). These are the primary interactive components that make team pages useful for comparing AI model performance per club.

Output: 4 new components in `src/components/team/`, 1 query extension in team-stats.ts, updated team detail page with leaderboard section and form indicator.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-foundation-data-layer/67-02-SUMMARY.md
@.planning/phases/68-routes-seo-basic-pages/68-02-SUMMARY.md
@src/app/teams/[slug]/page.tsx
@src/lib/db/queries/team-stats.ts
@src/lib/db/queries/stats.ts
@src/components/leaderboard-table.tsx
@src/components/leaderboard-filters.tsx
@src/components/accuracy-chart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Team leaderboard query, form indicator, and stats overview components</name>
  <files>
    src/lib/db/queries/team-stats.ts
    src/components/team/team-form-indicator.tsx
    src/components/team/team-stats-overview.tsx
  </files>
  <action>
**1. Extend `src/lib/db/queries/team-stats.ts` with `getTeamModelLeaderboard()`:**

Add a new exported async function that wraps the existing `getLeaderboardWithTrends()` from `stats.ts`:

```typescript
import { getLeaderboardWithTrends } from './stats';
import { format, subDays } from 'date-fns';

export type TeamLeaderboardPeriod = 'all' | 'season' | 'monthly' | 'weekly';

export async function getTeamModelLeaderboard(
  teamName: string,
  options?: {
    timePeriod?: TeamLeaderboardPeriod;
    limit?: number;
  }
) {
  const timePeriod = options?.timePeriod ?? 'all';

  // Map 'season' to dateFrom (current season start ~August)
  // For 'weekly' and 'monthly', use the existing timePeriod support in getLeaderboardWithTrends
  let mappedTimePeriod: 'all' | 'weekly' | 'monthly' = 'all';
  let dateFrom: string | undefined;

  if (timePeriod === 'weekly') {
    mappedTimePeriod = 'weekly';
  } else if (timePeriod === 'monthly') {
    mappedTimePeriod = 'monthly';
  } else if (timePeriod === 'season') {
    // Current season start: August 1 of current or previous year
    const now = new Date();
    const seasonYear = now.getMonth() >= 7 ? now.getFullYear() : now.getFullYear() - 1;
    dateFrom = `${seasonYear}-08-01`;
  }

  return getLeaderboardWithTrends(options?.limit ?? 42, 'avgPoints', {
    clubId: teamName,
    timePeriod: mappedTimePeriod,
    dateFrom,
  });
}
```

Import `getLeaderboardWithTrends` from `./stats` (same directory, no barrel file issue). Import `format, subDays` from `date-fns` only if needed (season uses a simple string).

**2. Create `src/components/team/team-form-indicator.tsx`:**

Client component ('use client') that renders W/D/L colored badges in a horizontal row.

Props: `{ form: ('W' | 'D' | 'L')[] }` -- form array comes from `getTeamFormGuide()` which returns oldest-first chronological order.

Implementation:
- Render form array left-to-right (already chronological from data layer)
- Each badge is a `div` with `h-8 w-8 rounded-md flex items-center justify-center font-semibold text-xs`
- Colors: W = `bg-green-500/20 text-green-400 border border-green-500/30`, D = `bg-yellow-500/20 text-yellow-400 border border-yellow-500/30`, L = `bg-red-500/20 text-red-400 border border-red-500/30`
- Use `cn()` from `@/lib/utils` for conditional classes
- Add `title` attribute on each badge: "Win", "Draw", "Loss"
- Export as named export `TeamFormIndicator`

**3. Create `src/components/team/team-stats-overview.tsx`:**

Server component (NO 'use client') that renders enhanced stats cards. This replaces the inline stats cards currently in page.tsx.

Props: `{ stats: TeamStats }` -- import `TeamStats` type from `@/lib/db/queries/team-stats`.

Render a grid of cards (same pattern as current page.tsx but enhanced):
- Row 1 (4 cards): W-D-L record, Goals Scored, Goals Conceded, Clean Sheets (same as current)
- Row 2 (4 cards): Goal Difference (with +/- coloring: green if positive, red if negative), Avg Goals Scored, Avg Goals Conceded, Win Rate (% = wins/totalMatches * 100, rounded)
- Row 3 (3 cards): Home record (homeWins-homeDraws-homeLosses), Away record (awayWins-awayDraws-awayLosses), Total Matches

Use `Card, CardContent` from `@/components/ui/card`. Same styling: `bg-card/50 border-border/50` on cards, `text-2xl font-bold` for values, `text-sm text-muted-foreground` for labels.

Export as named export `TeamStatsOverview`.
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | grep -E "team-stats|team-form|team-stats-overview" | head -20` -- should show no new errors from these files (pre-existing errors in other files are expected).

Verify exports: `grep -c "export" src/lib/db/queries/team-stats.ts` should show increased export count, `grep "export function TeamFormIndicator" src/components/team/team-form-indicator.tsx` and `grep "export function TeamStatsOverview" src/components/team/team-stats-overview.tsx` both return matches.
  </verify>
  <done>
- `getTeamModelLeaderboard()` exported from team-stats.ts and wraps getLeaderboardWithTrends with clubId filter
- TeamFormIndicator component renders W/D/L badges with correct colors
- TeamStatsOverview component renders 11 stat cards including home/away splits, averages, win rate
  </done>
</task>

<task type="auto">
  <name>Task 2: Team model leaderboard table with time filter and page integration</name>
  <files>
    src/components/team/team-model-leaderboard.tsx
    src/components/team/team-leaderboard-filter.tsx
    src/app/teams/[slug]/page.tsx
  </files>
  <action>
**1. Create `src/components/team/team-leaderboard-filter.tsx`:**

Client component ('use client') for time period selection. Follows the URL search params pattern from `leaderboard-filters.tsx`.

Props: `{ teamSlug: string }` (needed to construct the push URL).

Implementation:
- Use `useRouter()` and `useSearchParams()` from `next/navigation`
- Read current period from `searchParams.get('timePeriod') || 'all'`
- TIME_PERIODS array: `[{ value: 'all', label: 'All Time' }, { value: 'season', label: 'This Season' }, { value: 'monthly', label: 'Last 30 Days' }, { value: 'weekly', label: 'Last 7 Days' }]`
- On change: update URL with `router.push(\`/teams/${teamSlug}?timePeriod=${value}\`, { scroll: false })` -- delete timePeriod param if value is 'all'
- Render using `Select, SelectTrigger, SelectValue, SelectContent, SelectItem` from `@/components/ui/select`
- Style: `w-[180px] bg-card/50 border-border/50` on trigger (matches leaderboard-filters.tsx)
- Include Calendar icon from lucide-react before the select
- Export as named export `TeamLeaderboardFilter`

**2. Create `src/components/team/team-model-leaderboard.tsx`:**

Client component ('use client') rendering a TanStack Table with team-scoped model leaderboard data.

Props:
```typescript
interface TeamModelLeaderboardProps {
  entries: Array<{
    rank: number;
    modelId: string;
    displayName: string;
    provider: string;
    totalPredictions: number;
    totalPoints: number;
    avgPoints: number;
    exactScores: number;
    correctTendencies: number;
    accuracy: number;
    trendDirection: 'rising' | 'falling' | 'stable' | 'new';
    rankChange: number;
  }>;
}
```

Implementation:
- Import `useReactTable, getCoreRowModel, getSortedRowModel, flexRender, SortingState, ColumnDef` from `@tanstack/react-table`
- Define columns: Rank (#), Model Name (linked to `/models/${modelId}` if that route exists, otherwise plain text), Avg Points (bold, primary metric), Accuracy (%), Exact Scores, Total Predictions, Trend (arrow up/down/minus icon)
- Default sort by rank ascending (data comes pre-sorted by avgPoints desc)
- Render desktop table in `<div className="hidden md:block overflow-x-auto">` and mobile card view in `<div className="md:hidden space-y-3 p-4">` -- follow the dual-layout pattern from `leaderboard-table.tsx`
- Desktop table: `<table className="w-full">` with `<thead>` sticky header, sortable column headers with ArrowUpDown icon
- Mobile cards: Show rank, model name, avg points, accuracy for each entry
- Trend indicator: green ArrowUp for rising, red ArrowDown for falling, gray Minus for stable, sparkle/star for new
- If entries is empty, show "No predictions for this team yet" message
- Use `cn()` for conditional styling
- Export as named export `TeamModelLeaderboard`

**3. Update `src/app/teams/[slug]/page.tsx`:**

Modify the existing team detail page to integrate new components. This is the key wiring task.

Changes:
- Add `Suspense` import from 'react'
- Import new components: `TeamModelLeaderboard` from `@/components/team/team-model-leaderboard`, `TeamLeaderboardFilter` from `@/components/team/team-leaderboard-filter`, `TeamFormIndicator` from `@/components/team/team-form-indicator`, `TeamStatsOverview` from `@/components/team/team-stats-overview`
- Import `getTeamModelLeaderboard, getTeamFormGuide` from `@/lib/db/queries/team-stats`
- Add `searchParams: Promise<{ [key: string]: string | string[] | undefined }>` to PageProps interface
- Accept `searchParams` in both `generateMetadata` and default export
- Parse `timePeriod` from searchParams: `const timePeriod = typeof resolvedSearchParams.timePeriod === 'string' ? resolvedSearchParams.timePeriod as TeamLeaderboardPeriod : 'all'`
- Extend the Promise.all data fetch to include `getTeamModelLeaderboard(team.id, { timePeriod })` and `getTeamFormGuide(team.id, 5)`
- Replace the inline stats cards grid with `<TeamStatsOverview stats={stats} />`
- Add a new "Form" section after stats: heading "Recent Form" + `<TeamFormIndicator form={formGuide} />`
- Add a new "Model Leaderboard" section with heading "AI Model Leaderboard for {team.id}", the `<TeamLeaderboardFilter teamSlug={team.slug} />` below the heading, and `<TeamModelLeaderboard entries={leaderboard} />` below the filter
- Keep the existing "Recent Matches" section but enhance each match card to show a W/D/L result badge next to the score (green/yellow/red circle based on result from team's perspective)
- Section ordering on page: Header > Stats Overview > Form > Model Leaderboard > Recent Matches
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | grep "teams/\[slug\]/page" | head -10` -- should show no new type errors.

Verify all imports resolve: `grep -c "TeamModelLeaderboard\|TeamLeaderboardFilter\|TeamFormIndicator\|TeamStatsOverview\|getTeamModelLeaderboard\|getTeamFormGuide" src/app/teams/\[slug\]/page.tsx` should return 6+ matches.

Verify the page accepts searchParams: `grep "searchParams" src/app/teams/\[slug\]/page.tsx` should show searchParams in PageProps and function signature.

Verify leaderboard data flows: `grep "getTeamModelLeaderboard" src/app/teams/\[slug\]/page.tsx` returns a match showing the data fetch.
  </verify>
  <done>
- Team detail page shows TeamStatsOverview with 11 stat cards including home/away splits
- Team detail page shows TeamFormIndicator with colored W/D/L badges for last 5 matches
- Team detail page shows TeamModelLeaderboard with sortable TanStack Table
- TeamLeaderboardFilter updates URL ?timePeriod= param, page re-fetches with filtered leaderboard data
- All time periods work: all-time (default), season, monthly, weekly
- Desktop shows table view, mobile shows card view for leaderboard
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` shows no new errors from Phase 69 files
2. All 4 new component files exist: `ls src/components/team/team-{model-leaderboard,leaderboard-filter,form-indicator,stats-overview}.tsx`
3. Team page imports all components: `grep -c "from.*@/components/team" src/app/teams/\[slug\]/page.tsx` >= 4
4. Time filter syncs to URL: TeamLeaderboardFilter uses `useSearchParams` + `router.push`
5. Leaderboard query filters by team: `grep "clubId" src/lib/db/queries/team-stats.ts` returns match
6. Form indicator renders W/D/L: `grep "bg-green-500" src/components/team/team-form-indicator.tsx` returns match
</verification>

<success_criteria>
- STAT-01: Team page displays per-club model leaderboard ranked by average points with all active models
- STAT-02: Time period filter (all-time, season, monthly, weekly) changes leaderboard data via URL params
- STAT-03: Enhanced stats section shows W/D/L, goals, averages, home/away splits, win rate, clean sheets
- MTCH-03: Form indicator shows last 5 matches as colored W/D/L badges in chronological order
- All components follow existing codebase patterns (TanStack Table, Radix Select, Card components)
- Zero new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/69-ui-components-match-display/69-01-SUMMARY.md`
</output>
