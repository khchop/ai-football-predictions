---
phase: 76-lcp-font-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/providers.tsx
autonomous: true
requirements:
  - LCP-01
  - LCP-02

must_haves:
  truths:
    - "Homepage mobile LCP drops from 4.2s to < 2.5s"
    - "Hero <p> tag renders in first paint without blocking on font or JS hydration"
    - "next/font Inter uses display:swap and preload for immediate font availability"
    - "Critical text visible before full font loads (FOUT acceptable vs FOIT)"
    - "Hero section HTML delivered inline, not waiting for client components"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with optimized Inter font config"
      contains: "preload: true"
      contains: "display: 'swap'"
    - path: "src/app/providers.tsx"
      provides: "Client providers restructured to not block first paint"
      optimization: "hero renders before hydration"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/providers.tsx"
      via: "Providers wrapping children"
      issue: "client component blocks first paint"
    - from: "next/font"
      to: "hero <p> tag"
      via: "font-display: swap"
      optimization: "text visible immediately"
---

<objective>
Reduce homepage mobile LCP from 4.2s to < 2.5s by optimizing font loading and eliminating hydration blocking on hero section.

Purpose: The hero `<p>` tag is currently the LCP element but is blocked by font loading and client component hydration. This plan optimizes both to achieve target LCP.

Output: Optimized layout.tsx and providers.tsx with explicit font preload and non-blocking client components.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 76 LCP & Font Optimization)
@.planning/STATE.md (v3.2 PageSpeed Optimization context)
@.planning/REQUIREMENTS.md (LCP-01, LCP-02)

Current state from investigation:
- src/app/layout.tsx: Inter font configured with only subsets: ["latin"], missing explicit display: swap and preload
- src/app/providers.tsx: Client component ('use client') wrapping entire app children
- The hero <p> tag at page.tsx line 346-349 is the LCP element
- ThemeProvider from next-themes requires client-side rendering but should not block initial HTML paint

Key insight: The Providers wrapper is causing the hero to wait for client-side hydration. The font also needs explicit preload configuration.
</context>

<tasks>

<task type="auto">
  <name>Optimize next/font Inter configuration</name>
  <files>src/app/layout.tsx</files>
  <action>
    Modify the Inter font configuration in layout.tsx to add explicit optimization:
    1. Change line 15-17 from:
       ```
       const inter = Inter({
         subsets: ["latin"],
       });
       ```
    2. To:
       ```
       const inter = Inter({
         subsets: ["latin"],
         display: 'swap',
         preload: true,
       });
       ```
    3. This ensures the font is preloaded in the document <head> and uses swap display to show fallback text immediately while font loads.

    WHY: The LCP element (hero <p> tag) is blocked waiting for font to load. Explicit display:swap shows system font immediately (FOUT), and preload: true loads font early in the critical path.
  </action>
  <verify>
    Build the project and check that:
    1. The Inter font link appears in <head> with rel="preload"
    2. Run: npm run build 2>&1 | grep -i "font" (should show preload)
    3. Or check the generated HTML has font preloading
  </verify>
  <done>
    Inter font configured with display: 'swap' and preload: true in layout.tsx. Font preloads in document head, text visible immediately with fallback font until Inter loads.
  </done>
</task>

<task type="auto">
  <name>Restructure Providers to eliminate hydration blocking</name>
  <files>src/app/providers.tsx, src/app/layout.tsx</files>
  <action>
    Restructure the Providers component to not block first paint of the hero section:

    Option A (preferred): Make Providers accept a generic ReactNode and render children directly (already done), but move the ThemeProvider to only wrap client-interactive parts. However, since next-themes requires client-side, we need a different approach.

    Option B: Ensure the hero HTML is delivered as static HTML before client hydration kicks in. The current setup already does this - the HTML is SSR and sent to browser. The issue might be that the Providers wrapper creates a boundary that delays streaming.

    Option C (simplest): Keep Providers as-is but verify the hero section is NOT wrapped in any client Suspense boundary. The hero at page.tsx lines 338-350 is already in the main server component and NOT wrapped in Suspense - this is correct.

    Actual fix needed: Move Providers OUTSIDE the main content area to only wrap interactive components. Currently:
    ```
    <Providers>
      <TooltipProvider>
        <Navigation />
        <main>{children}</main>
        <Footer />
        <BottomNav />
      </TooltipProvider>
    </Providers>
    ```

    Refactor to:
    ```
    <TooltipProvider>
      <Navigation />
      <main>{children}</main>
      <Footer />
      <BottomNav />
    </TooltipProvider>
    ```

    And move ThemeProvider (Providers) to only wrap components that need theme context. Or use suppressHydrationWarning on the html tag (already present) and defer theme application.

    Step 1: Modify layout.tsx to remove Providers wrapper from around the main content area
    Step 2: Keep only TooltipProvider (which is a client component but lightweight)
    Step 3: Apply ThemeProvider context directly to body class instead of wrapping children
    Step 4: Remove 'use client' from providers.tsx and make it a server component that composes client parts
  </action>
  <verify>
    Test by running lighthouse or checking:
    1. Build succeeds: npm run build
    2. Check that hero HTML renders without waiting for client JS - view-source on homepage should show hero <p> immediately
    3. Check no blocking Suspense around hero in layout
  </verify>
  <done>
    Hero section renders as inline HTML in first paint, not blocked by client component hydration. Providers restructured to minimize hydration delay.
  </done>
</task>

</tasks>

<verification>
- Lighthouse mobile LCP should show < 2.5s (was 4.2s)
- Hero <p> tag should be the LCP element but should render in first paint
- Font preloading visible in network tab
- No blocking hydration before first paint
</verification>

<success_criteria>
1. Homepage mobile LCP < 2.5s (measured via Lighthouse)
2. Hero <p> tag visible in first paint (no blank screen waiting for font/JS)
3. Inter font preloaded with display: swap
4. Critical text visible immediately (system font fallback until Inter loads)
5. Hero HTML delivered inline, not waiting for client components
</success_criteria>

<output>
After completion, create `.planning/phases/76-lcp-font-optimization/76-01-SUMMARY.md`
</output>
