---
phase: 68-routes-seo-basic-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/metadata.ts
  - src/lib/navigation/breadcrumb-utils.ts
  - src/lib/seo/schema/team.ts
  - src/app/api/og/team/route.tsx
  - src/app/sitemap/teams.xml/route.ts
autonomous: true

must_haves:
  truths:
    - "buildTeamTitle() and buildTeamDescription() produce correctly formatted SEO strings"
    - "buildSportsTeamSchema() produces valid Schema.org SportsTeam JSON-LD"
    - "buildTeamBreadcrumbs() returns breadcrumb array matching Home > Teams > Club Name"
    - "OG image route at /api/og/team renders 1200x630 image with team name and W/D/L stats"
    - "Teams sitemap only includes teams with 5+ finished matches"
  artifacts:
    - path: "src/lib/seo/metadata.ts"
      provides: "buildTeamTitle, buildTeamDescription helpers"
      contains: "buildTeamTitle"
    - path: "src/lib/seo/schema/team.ts"
      provides: "SportsTeam schema builder"
      exports: ["buildSportsTeamSchema"]
    - path: "src/lib/navigation/breadcrumb-utils.ts"
      provides: "Team breadcrumb builder"
      contains: "buildTeamBreadcrumbs"
    - path: "src/app/api/og/team/route.tsx"
      provides: "Team OG image API"
      exports: ["GET"]
    - path: "src/app/sitemap/teams.xml/route.ts"
      provides: "Team sitemap with quality filter"
      exports: ["GET"]
  key_links:
    - from: "src/lib/seo/schema/team.ts"
      to: "src/lib/football/teams.ts"
      via: "TeamConfig type import"
      pattern: "import.*TeamConfig.*from.*teams"
    - from: "src/app/sitemap/teams.xml/route.ts"
      to: "src/lib/db/queries/team-stats.ts"
      via: "getTeamStats for quality filtering"
      pattern: "getTeamStats"
---

<objective>
Create all SEO infrastructure needed by team pages: metadata helpers, SportsTeam schema builder, breadcrumb builder, OG image API route, and teams sitemap with quality filtering.

Purpose: Phase 68 team pages require these SEO primitives before the actual page components can be built. Separating infrastructure from pages allows Plan 02 to focus purely on page assembly.

Output: 5 files (3 extended, 2 new) providing complete SEO tooling for team pages.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/seo/metadata.ts
@src/lib/seo/schema/competition.ts
@src/lib/seo/schema/breadcrumb.ts
@src/lib/seo/constants.ts
@src/lib/navigation/breadcrumb-utils.ts
@src/lib/football/teams.ts
@src/lib/db/queries/team-stats.ts
@src/app/api/og/league/route.tsx
@src/app/sitemap/leagues.xml/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add team SEO helpers (metadata, schema, breadcrumbs)</name>
  <files>
    src/lib/seo/metadata.ts
    src/lib/seo/schema/team.ts
    src/lib/navigation/breadcrumb-utils.ts
  </files>
  <action>
**1. Extend `src/lib/seo/metadata.ts`** — Add two new functions following the exact pattern of `buildLeagueTitle()` and `buildLeagueDescription()`:

```typescript
export function buildTeamTitle(teamName: string): string {
  const base = `${teamName} AI Predictions & Stats`;
  const withBrand = `${base} | Kroam`;
  return withBrand.length <= MAX_TITLE_LENGTH ? withBrand : base;
}

export function buildTeamDescription(teamName: string, stats: { wins: number; draws: number; losses: number; totalMatches: number }, modelCount: number): string {
  const base = `${teamName} stats: ${stats.wins}W-${stats.draws}D-${stats.losses}L from ${stats.totalMatches} matches. AI predictions from ${modelCount} models on Kroam.`;
  return enforceDescriptionLength(base);
}
```

Import `TeamStats` type is NOT needed — use inline type to avoid circular dependency with db module. The stats parameter uses a minimal subset `{ wins, draws, losses, totalMatches }`.

**2. Create `src/lib/seo/schema/team.ts`** — New file following `competition.ts` pattern:

- Import `SportsTeam` type from `schema-dts`
- Import `BASE_URL` from `../constants`
- Import `TeamConfig` from `@/lib/football/teams`
- Import `COMPETITIONS` from `@/lib/football/competitions` (for memberOf)
- Export `buildSportsTeamSchema(team: TeamConfig, stats: { wins, draws, losses, goalsScored, totalMatches }): SportsTeam`
- Return object with:
  - `'@type': 'SportsTeam'`
  - `'@id': \`${BASE_URL}/teams/${team.slug}\``
  - `name: team.id`
  - `url: \`${BASE_URL}/teams/${team.slug}\``
  - `sport: 'Football'`
  - `description: \`${team.id} statistics: ${stats.wins}W-${stats.draws}D-${stats.losses}L with ${stats.goalsScored} goals scored across ${stats.totalMatches} matches.\``
  - `memberOf`: Look up competition via `COMPETITIONS.find(c => c.id === team.league)` and if found, include `{ '@type': 'SportsOrganization', name: competition.name, url: \`${BASE_URL}/leagues/${competition.id}\` }`

**3. Extend `src/lib/navigation/breadcrumb-utils.ts`** — Add `buildTeamBreadcrumbs` following the exact pattern of `buildLeagueBreadcrumbs`:

```typescript
export function buildTeamBreadcrumbs(
  teamName: string,
  _teamSlug: string
): BreadcrumbItem[] {
  return [
    { name: 'Home', href: '/' },
    { name: 'Teams', href: '/teams' },
    { name: teamName, href: '' }, // Current page
  ];
}
```
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors across modified files. Verify that `schema-dts` includes `SportsTeam` type (it does — it's part of the package).
  </verify>
  <done>
Three files contain new team SEO helpers: `buildTeamTitle()` and `buildTeamDescription()` in metadata.ts, `buildSportsTeamSchema()` exported from schema/team.ts, and `buildTeamBreadcrumbs()` in breadcrumb-utils.ts. All follow existing patterns from league equivalents.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create team OG image route and teams sitemap</name>
  <files>
    src/app/api/og/team/route.tsx
    src/app/sitemap/teams.xml/route.ts
  </files>
  <action>
**1. Create `src/app/api/og/team/route.tsx`** — Clone from `/api/og/league/route.tsx` with team-specific adaptations:

- Import `ImageResponse` from `next/og`
- Export async `GET(request: Request)` handler
- Parse search params: `teamName`, `wins`, `draws`, `losses`
- Return `new ImageResponse(...)` with 1200x630 dimensions
- Use same gradient background as league route: `linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%)`
- Layout: Team name (64px, bold, centered), then W/D/L stats row (32px) with green for wins, muted for draws, red for losses
- Include "Kroam.xyz" branding bottom-left and "AI Predictions" badge bottom-right (same as league route)
- Use a football emoji (instead of trophy for leagues) above team name
- Cache headers: `'Cache-Control': 'public, max-age=3600, s-maxage=3600, stale-while-revalidate=86400'` (matching league route)

**2. Create `src/app/sitemap/teams.xml/route.ts`** — Follow `leagues.xml/route.ts` pattern with content quality filtering:

- Import `BASE_URL` from `@/lib/seo/constants`
- Import `TEAMS` from `@/lib/football/teams`
- Import `getTeamStats` from `@/lib/db/queries/team-stats`
- Export async `GET(): Promise<Response>`
- For each team in TEAMS, call `getTeamStats(team.id)` — use `Promise.all` for parallel execution
- **Filter out teams with `stats.totalMatches < 5`** (thin content prevention)
- Generate XML sitemap entries with:
  - `loc`: `${BASE_URL}/teams/${team.slug}`
  - `lastmod`: today's date (ISO format, date only)
  - `changefreq`: `weekly` (team stats update less frequently than live matches)
  - `priority`: `0.7` (lower than leagues at 0.9)
- Return Response with `Content-Type: application/xml` and `Cache-Control: public, max-age=3600, s-maxage=3600`
- Important: Batch the getTeamStats calls rather than calling them sequentially. Use `Promise.all(TEAMS.map(...))` to parallelize.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify both files export a `GET` function.
  </verify>
  <done>
OG image route at `/api/og/team` renders dynamic team images with W/D/L stats. Teams sitemap at `/sitemap/teams.xml` only includes teams with 5+ finished matches, preventing thin content penalties.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `src/lib/seo/metadata.ts` exports `buildTeamTitle` and `buildTeamDescription`
3. `src/lib/seo/schema/team.ts` exports `buildSportsTeamSchema`
4. `src/lib/navigation/breadcrumb-utils.ts` exports `buildTeamBreadcrumbs`
5. `src/app/api/og/team/route.tsx` exports `GET`
6. `src/app/sitemap/teams.xml/route.ts` exports `GET` and filters teams with <5 matches
</verification>

<success_criteria>
All 5 SEO infrastructure files exist and compile. Team metadata helpers follow established patterns from league equivalents. SportsTeam schema uses schema-dts types. OG route generates 1200x630 images. Sitemap enforces 5-match minimum threshold.
</success_criteria>

<output>
After completion, create `.planning/phases/68-routes-seo-basic-pages/68-01-SUMMARY.md`
</output>
