---
phase: 10-page-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/match/match-h1.tsx
  - src/app/leagues/[slug]/[match]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Match detail pages render exactly one H1 tag"
    - "H1 content includes both team names"
    - "H1 is accessible to screen readers"
    - "Finished matches show score in H1"
    - "Upcoming matches show 'vs' format in H1"
  artifacts:
    - path: "src/components/match/match-h1.tsx"
      provides: "Semantic H1 component for match pages"
      exports: ["MatchH1"]
      min_lines: 20
    - path: "src/app/leagues/[slug]/[match]/page.tsx"
      provides: "Match detail page with H1"
      contains: "<MatchH1"
  key_links:
    - from: "src/app/leagues/[slug]/[match]/page.tsx"
      to: "src/components/match/match-h1.tsx"
      via: "import and render"
      pattern: "import.*MatchH1.*from"
---

<objective>
Add semantic H1 tags to all match detail pages to fix the 161 pages missing H1 issue identified in Ahrefs audit.

Purpose: SEO-T05 requires every page to have a single H1 tag for proper content hierarchy and accessibility. Match pages currently have visual headers but no semantic H1.

Output: MatchH1 component with sr-only visibility, integrated into match detail page.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-page-structure/10-RESEARCH.md

# Existing patterns to follow
@src/components/competition/competition-header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MatchH1 component</name>
  <files>src/components/match/match-h1.tsx</files>
  <action>
Create a new MatchH1 component following the pattern from research:

```typescript
interface MatchH1Props {
  homeTeam: string;
  awayTeam: string;
  status: string;
  homeScore: number | null;
  awayScore: number | null;
}

export function MatchH1({ homeTeam, awayTeam, status, homeScore, awayScore }: MatchH1Props)
```

Implementation:
1. For finished matches with scores: `{homeTeam} {homeScore}-{awayScore} {awayTeam} Match Report`
2. For upcoming/live matches: `{homeTeam} vs {awayTeam} AI Predictions`
3. Use Tailwind `sr-only` class for accessibility (visible to screen readers, not visual users)
4. Export component as named export

Do NOT:
- Make the H1 visually displayed (use sr-only)
- Add any additional styling beyond sr-only
- Include competition name in H1 (keep it concise)
  </action>
  <verify>
- File exists at src/components/match/match-h1.tsx
- TypeScript compiles: `npx tsc --noEmit src/components/match/match-h1.tsx`
- Component exports MatchH1 function
  </verify>
  <done>MatchH1 component exists with proper TypeScript types and sr-only accessibility class</done>
</task>

<task type="auto">
  <name>Task 2: Integrate MatchH1 into match detail page</name>
  <files>src/app/leagues/[slug]/[match]/page.tsx</files>
  <action>
Add the MatchH1 component to the match detail page:

1. Import MatchH1 from '@/components/match/match-h1'
2. Add MatchH1 as the first element inside the main container div (after schemas, before back link)
3. Pass match data props:
   - homeTeam: matchData.homeTeam
   - awayTeam: matchData.awayTeam
   - status: matchData.status
   - homeScore: matchData.homeScore
   - awayScore: matchData.awayScore

Position in JSX (after line 168):
```jsx
<div className="max-w-4xl mx-auto space-y-8">
  <SportsEventSchema match={matchData} />
  <WebPageSchema ... />
  <MatchH1
    homeTeam={matchData.homeTeam}
    awayTeam={matchData.awayTeam}
    status={matchData.status}
    homeScore={matchData.homeScore}
    awayScore={matchData.awayScore}
  />
  <Link href={...} ... />
  ...
```

Do NOT:
- Move or modify the existing SportsEventSchema or WebPageSchema
- Change any other part of the page structure
- Add the H1 to the /matches/[id]/page.tsx (that page redirects to /leagues route)
  </action>
  <verify>
- grep -n "MatchH1" src/app/leagues/[slug]/[match]/page.tsx shows import and usage
- npm run build passes without errors
- Dev server renders match page: npm run dev, visit /leagues/premier-league
  </verify>
  <done>Match detail pages render MatchH1 component as first semantic element</done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. Match page has exactly one H1: Use browser dev tools to inspect /leagues/premier-league/{any-match}
3. H1 is sr-only (not visually displayed but accessible)
4. H1 content matches expected format for finished/upcoming matches
</verification>

<success_criteria>
- SEO-T05 addressed: All match pages have H1 tag
- H1 uses sr-only class for accessibility compliance
- No visual changes to match pages
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-structure/10-01-SUMMARY.md`
</output>
