---
phase: 63-model-consolidation-execution
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Forward migration executed successfully on production database"
    - "Zero orphaned foreign keys across all 5 referencing tables"
    - "Zero old -syn model_id references remain for 3 consolidated models"
    - "Leaderboard recalculates correctly after cache invalidation"
    - "All model-keyed caches invalidated after migration"
  artifacts: []
  key_links:
    - from: "scripts/migrate-consolidate-models.ts"
      to: "production database"
      via: "DATABASE_URL connection"
      pattern: "Migration committed successfully"
    - from: "scripts/post-consolidation.ts"
      to: "production database + Redis"
      via: "DATABASE_URL + REDIS_URL"
      pattern: "PASS.*orphaned"
---

<objective>
Execute model consolidation migration on production, deploy code changes, and verify end-to-end integrity.

Purpose: This plan executes the migration scripts (built in Phase 62) and code changes (built in 63-01) against production. It's the actual "flip the switch" -- merging 3 Synthetic model IDs into base routing IDs in the live database, deploying the updated provider configuration, and verifying everything works.

Output: Production database consolidated, caches invalidated, leaderboard recalculated, integrity verified
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-model-consolidation-execution/63-01-SUMMARY.md
@.planning/phases/63-model-consolidation-execution/63-RESEARCH.md
@scripts/migrate-consolidate-models.ts
@scripts/post-consolidation.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute forward migration on production database</name>
  <action>
Run the forward migration script built in Phase 62 against the production database. This requires DATABASE_URL to be set to the production PostgreSQL connection string.

**Why human required:** This modifies production data -- model_id foreign keys across 5 tables (predictions, llm_model_stats, bets, model_balances, model_usage). Claude cannot access production credentials.
  </action>
  <instructions>
**Step 1: Dry-run (preview changes)**
```bash
npx tsx scripts/migrate-consolidate-models.ts --dry-run --verbose
```
Review the output. Verify:
- Pre-validation shows expected row counts
- Conflict count is reasonable (Phase 62 dry-run showed 12 prediction conflicts)
- No unexpected errors
- Output ends with "DRY RUN: All changes rolled back"

**Step 2: Execute migration**
```bash
npx tsx scripts/migrate-consolidate-models.ts --verbose 2>&1 | tee logs/migration-consolidate-$(date +%Y%m%d-%H%M%S).log
```
Verify:
- Output shows "Migration committed successfully"
- Post-validation shows 0 unique constraint violations
- Orphaned FKs message shows "EXPECTED" (Phase 63 post-consolidation script will fix)

**Step 3: Run post-consolidation script**
```bash
npx tsx scripts/post-consolidation.ts --verbose
```
This deactivates old -syn model rows, invalidates all caches, and runs integrity verification.
Verify:
- All checks show PASS
- 0 orphaned FKs
- 0 remaining -syn references for consolidated models
- Cache invalidation reports keys deleted

If any step fails, the migration script's transaction ensures no partial changes. Run `npx tsx scripts/rollback-consolidate-models.ts --dry-run` to preview rollback if needed.
  </instructions>
  <resume-signal>Paste the output of the post-consolidation script verification section, or describe any issues encountered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy code changes and verify application</name>
  <what-built>
Code changes from Plan 63-01:
- MODEL_PROVIDER_ROUTES updated (removed -syn entries for 3 consolidated models)
- SYNTHETIC_PROVIDERS reduced from 13 to 10 providers
- Post-consolidation script executed (caches invalidated, old models deactivated)
  </what-built>
  <how-to-verify>
After deploying the code changes (git push triggers Coolify deployment):

1. **Verify leaderboard at kroam.xyz/leaderboard:**
   - Models deepseek-r1, kimi-k2-thinking, kimi-k2.5 should appear with combined prediction history
   - No duplicate entries (no -syn variants visible)
   - Accuracy percentages should be reasonable (aggregated from both providers)

2. **Verify admin dashboard at kroam.xyz/admin:**
   - Model count should show 39 active models (was 42)
   - Model health cards should NOT show deepseek-r1-0528-syn, kimi-k2-thinking-syn, or kimi-k2.5-syn
   - Provider distribution should still show Synthetic provider serving remaining 10 models

3. **Verify predictions still work:**
   - Check that upcoming match predictions generate successfully
   - New predictions should use base model IDs (deepseek-r1, not deepseek-r1-0528-syn)

4. **Verify no errors in logs:**
   - Check Coolify logs for any FK constraint errors or "model not found" errors
   - Model sync on startup should report 39 active models
  </how-to-verify>
  <resume-signal>Type "approved" if leaderboard, admin, and predictions look correct, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
1. Migration script ran successfully (output logged)
2. Post-consolidation script shows all PASS checks
3. Zero orphaned FKs across all tables
4. Zero -syn references for 3 consolidated model IDs in database
5. Leaderboard displays consolidated models with correct aggregates
6. Admin dashboard shows updated model count (39)
7. New predictions use base model IDs
</verification>

<success_criteria>
- Production database has 0 references to deepseek-r1-0528-syn, kimi-k2-thinking-syn, kimi-k2.5-syn in predictions/stats/bets/balances/usage tables
- Leaderboard aggregates recalculated correctly (cache invalidation triggers fresh calc)
- All 5 referencing tables have 0 orphaned foreign keys
- Application runs normally after deployment with 39 active models
</success_criteria>

<output>
After completion, create `.planning/phases/63-model-consolidation-execution/63-02-SUMMARY.md`
</output>
