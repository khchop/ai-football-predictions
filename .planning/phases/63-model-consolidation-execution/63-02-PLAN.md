---
phase: 63-model-consolidation-execution
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Forward migration executed successfully on production database"
    - "Rename migration executed successfully -- zero -syn model_ids remain in database"
    - "Zero orphaned foreign keys across all 5 referencing tables"
    - "Zero -syn model_id references remain for ANY model (consolidated or renamed)"
    - "Leaderboard recalculates correctly after cache invalidation"
    - "All model-keyed caches invalidated after migration"
  artifacts: []
  key_links:
    - from: "scripts/migrate-consolidate-models.ts"
      to: "production database"
      via: "DATABASE_URL connection"
      pattern: "Migration committed successfully"
    - from: "scripts/rename-syn-models.ts"
      to: "production database"
      via: "DATABASE_URL connection"
      pattern: "Rename committed successfully"
    - from: "scripts/post-consolidation.ts"
      to: "production database + Redis"
      via: "DATABASE_URL + REDIS_URL"
      pattern: "PASS.*orphaned"
---

<objective>
Execute model consolidation migration, rename migration, and post-consolidation on production. Deploy code changes and verify end-to-end integrity.

Purpose: This plan executes the migration scripts (built in Phase 62) and the rename migration + code changes (built in 63-01) against production. It's the actual "flip the switch" -- merging 3 Synthetic model IDs into base routing IDs, renaming 10 Synthetic-exclusive model IDs to drop -syn suffix, deploying the updated provider configuration, and verifying everything works.

Output: Production database consolidated and renamed, caches invalidated, leaderboard recalculated, integrity verified, zero -syn references remaining anywhere
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-model-consolidation-execution/63-01-SUMMARY.md
@.planning/phases/63-model-consolidation-execution/63-RESEARCH.md
@scripts/migrate-consolidate-models.ts
@scripts/rename-syn-models.ts
@scripts/post-consolidation.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute forward migration, rename migration, and post-consolidation on production</name>
  <action>
Run the forward migration script (Phase 62), the rename migration script (Phase 63), and the post-consolidation script against the production database. This requires DATABASE_URL to be set to the production PostgreSQL connection string.

**Why human required:** This modifies production data -- model_id foreign keys across 5 tables (predictions, llm_model_stats, bets, model_balances, model_usage) for both consolidation and rename operations. Claude cannot access production credentials.
  </action>
  <instructions>
**Step 1: Dry-run consolidation migration (preview changes)**
```bash
npx tsx scripts/migrate-consolidate-models.ts --dry-run --verbose
```
Review the output. Verify:
- Pre-validation shows expected row counts
- Conflict count is reasonable (Phase 62 dry-run showed 12 prediction conflicts)
- No unexpected errors
- Output ends with "DRY RUN: All changes rolled back"

**Step 2: Execute consolidation migration**
```bash
npx tsx scripts/migrate-consolidate-models.ts --verbose 2>&1 | tee logs/migration-consolidate-$(date +%Y%m%d-%H%M%S).log
```
Verify:
- Output shows "Migration committed successfully"
- Post-validation shows 0 unique constraint violations

**Step 3: Dry-run rename migration (preview changes)**
```bash
npx tsx scripts/rename-syn-models.ts --dry-run --verbose
```
Review the output. Verify:
- Pre-validation shows row counts per model for each of the 10 models being renamed
- Target model IDs do not already exist
- Output ends with "DRY RUN: All changes rolled back"

**Step 4: Execute rename migration**
```bash
npx tsx scripts/rename-syn-models.ts --verbose 2>&1 | tee logs/migration-rename-syn-$(date +%Y%m%d-%H%M%S).log
```
Verify:
- Output shows "Rename committed successfully"
- Post-validation shows 0 old -syn model_ids remain
- Row counts match pre-validation expectations

**Step 5: Run post-consolidation script**
```bash
npx tsx scripts/post-consolidation.ts --verbose
```
This deactivates old -syn model rows (3 consolidated), invalidates all caches, and runs integrity verification.
Verify:
- All checks show PASS
- 0 orphaned FKs
- 0 remaining -syn references for ANY model in database
- Cache invalidation reports keys deleted

**Execution order matters:**
1. Consolidation migration FIRST (merges 3 models)
2. Rename migration SECOND (renames remaining 10 models)
3. Post-consolidation THIRD (cleanup + verification)

If any step fails, the migration script's transaction ensures no partial changes. Run `npx tsx scripts/rollback-consolidate-models.ts --dry-run` to preview rollback if needed (only applicable to step 2).
  </instructions>
  <resume-signal>Paste the output of the post-consolidation script verification section, or describe any issues encountered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy code changes and verify application</name>
  <what-built>
Code changes from Plan 63-01:
- MODEL_PROVIDER_ROUTES updated (removed -syn entries for 3 consolidated models)
- SYNTHETIC_PROVIDERS reduced from 13 to 10 providers with clean IDs (no -syn suffix)
- All 10 Synthetic-exclusive models renamed: dropped -syn suffix from provider IDs and display names
- Post-consolidation script executed (caches invalidated, old models deactivated)
- Rename migration executed (10 Synthetic-exclusive model IDs updated in database)
  </what-built>
  <how-to-verify>
After deploying the code changes (git push triggers Coolify deployment):

1. **Verify leaderboard at kroam.xyz/leaderboard:**
   - Models deepseek-r1, kimi-k2-thinking, kimi-k2.5 should appear with combined prediction history
   - No duplicate entries (no -syn variants visible)
   - Renamed models (e.g., qwen3-235b-thinking, deepseek-v3-0324, minimax-m2) should appear with CLEAN names (no -syn suffix, no "(Synthetic)" label)
   - Accuracy percentages should be reasonable

2. **Verify admin dashboard at kroam.xyz/admin:**
   - Model count should show 39 active models (was 42)
   - Model health cards should NOT show ANY -syn suffixed model IDs
   - Provider distribution should still show Synthetic provider serving 10 models

3. **Verify predictions still work:**
   - Check that upcoming match predictions generate successfully
   - New predictions should use clean model IDs (e.g., 'qwen3-235b-thinking' not 'qwen3-235b-thinking-syn')

4. **Verify no errors in logs:**
   - Check Coolify logs for any FK constraint errors or "model not found" errors
   - Model sync on startup should report 39 active models

5. **Verify zero -syn references remain:**
   - Run in database: `SELECT DISTINCT model_id FROM predictions WHERE model_id LIKE '%-syn' LIMIT 5;` -- should return 0 rows
   - Run: `SELECT id FROM models WHERE id LIKE '%-syn' AND active = true LIMIT 5;` -- should return 0 rows
  </how-to-verify>
  <resume-signal>Type "approved" if leaderboard, admin, predictions, and -syn cleanup all look correct, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
1. Consolidation migration script ran successfully (output logged)
2. Rename migration script ran successfully (output logged)
3. Post-consolidation script shows all PASS checks
4. Zero orphaned FKs across all tables
5. Zero -syn model_id references remain in entire database (not just consolidated 3, but ALL 13)
6. Leaderboard displays consolidated models with correct aggregates
7. Leaderboard displays renamed models with clean names
8. Admin dashboard shows updated model count (39)
9. New predictions use clean model IDs (no -syn suffix)
</verification>

<success_criteria>
- Production database has 0 references to ANY -syn model_id in predictions/stats/bets/balances/usage tables
- 3 consolidated model IDs merged into base routing IDs (deepseek-r1, kimi-k2-thinking, kimi-k2.5)
- 10 Synthetic-exclusive model IDs renamed without -syn suffix
- Leaderboard aggregates recalculated correctly (cache invalidation triggers fresh calc)
- All 5 referencing tables have 0 orphaned foreign keys
- Application runs normally after deployment with 39 active models
</success_criteria>

<output>
After completion, create `.planning/phases/63-model-consolidation-execution/63-02-SUMMARY.md`
</output>
