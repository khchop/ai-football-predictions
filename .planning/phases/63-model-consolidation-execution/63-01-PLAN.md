---
phase: 63-model-consolidation-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/llm/index.ts
  - src/lib/llm/providers/synthetic.ts
  - scripts/post-consolidation.ts
autonomous: true

must_haves:
  truths:
    - "MODEL_PROVIDER_ROUTES no longer references -syn provider IDs for 3 consolidated models"
    - "SYNTHETIC_PROVIDERS array contains only 10 Synthetic-exclusive models (not 13)"
    - "Post-consolidation script exists to invalidate caches, deactivate old models, and verify integrity"
    - "Application builds successfully with updated provider configuration"
  artifacts:
    - path: "src/lib/llm/index.ts"
      provides: "Updated MODEL_PROVIDER_ROUTES without -syn entries for 3 consolidated models"
      contains: "'deepseek-r1': \\['deepseek-r1'"
    - path: "src/lib/llm/providers/synthetic.ts"
      provides: "10 Synthetic-exclusive providers (3 consolidated providers removed)"
      contains: "10 SYNTHETIC-EXCLUSIVE MODELS"
    - path: "scripts/post-consolidation.ts"
      provides: "Cache invalidation, model deactivation, and integrity verification"
      exports: ["main"]
  key_links:
    - from: "src/lib/llm/index.ts"
      to: "src/lib/llm/providers/synthetic.ts"
      via: "SYNTHETIC_PROVIDERS import"
      pattern: "import.*SYNTHETIC_PROVIDERS"
    - from: "src/lib/llm/index.ts"
      to: "MODEL_PROVIDER_ROUTES validation"
      via: "validateProviderRoutes at module load"
      pattern: "validateProviderRoutes"
---

<objective>
Update application code to reflect 3-model consolidation and create post-migration operational script.

Purpose: After Phase 62 built the database migration scripts, Phase 63 needs code changes so the application treats consolidated models correctly going forward. The 3 Synthetic provider definitions that were merged into base IDs (deepseek-r1-0528-syn, kimi-k2-thinking-syn, kimi-k2.5-syn) must be removed from provider arrays and routes. A post-consolidation script handles cache invalidation, model deactivation, and integrity verification for production execution.

Output: Updated provider configuration files + post-consolidation operational script
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-migration-script-development/62-01-SUMMARY.md
@.planning/phases/63-model-consolidation-execution/63-RESEARCH.md
@src/lib/llm/index.ts
@src/lib/llm/providers/synthetic.ts
@src/lib/cache/redis.ts
@src/lib/db/sync-models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove consolidated -syn providers from code configuration</name>
  <files>src/lib/llm/providers/synthetic.ts, src/lib/llm/index.ts</files>
  <action>
**In `src/lib/llm/providers/synthetic.ts`:**

1. Remove the 3 consolidated provider INSTANCES (keep the class and remaining 10 providers):
   - Remove `DeepSeekR1_0528_SynProvider` (id: 'deepseek-r1-0528-syn') -- lines ~89-102
   - Remove `KimiK2Thinking_SynProvider` (id: 'kimi-k2-thinking-syn') -- lines ~105-118
   - Remove `KimiK25_SynProvider` (id: 'kimi-k2.5-syn') -- lines ~212-225

2. Update `SYNTHETIC_PROVIDERS` array export:
   - Remove the 3 entries: `DeepSeekR1_0528_SynProvider`, `KimiK2Thinking_SynProvider`, `KimiK25_SynProvider`
   - Update the section header comment to say "10 SYNTHETIC-EXCLUSIVE MODELS" (not 13)
   - Update numbering comments (items now 1-10 instead of 1-13)
   - Update summary comment at bottom: "10 Synthetic-exclusive models"

3. Update the reasoning models section header: "REASONING MODELS (1)" since only Qwen3 235B Thinking remains in reasoning category. The Moonshot section that had Kimi K2.5 should be removed entirely.

**In `src/lib/llm/index.ts`:**

1. Update `MODEL_PROVIDER_ROUTES`:
   - `'deepseek-r1'`: Change from `['deepseek-r1-0528-syn', 'deepseek-r1', 'deepseek-r1-or']` to `['deepseek-r1', 'deepseek-r1-or']`
   - `'kimi-k2-thinking'`: Change from `['kimi-k2-thinking-syn', 'kimi-k2-instruct']` to `['kimi-k2-instruct']` (single provider, no route needed -- REMOVE this entry entirely since only models with 2+ providers get entries per Phase 60 decision)
   - `'kimi-k2.5'`: Change from `['kimi-k2.5-syn', 'kimi-k2-instruct']` to `['kimi-k2-instruct']` (single provider -- REMOVE this entry entirely per same decision)

2. Update comment: "Together: 29 models, Synthetic: 10 exclusive models = 39 total" (was 42 total with 13 Synthetic)

3. Update the route section comments:
   - `'deepseek-r1'` moves from "3-tier: Synthetic -> Together -> OpenRouter" to "2-tier: Together -> OpenRouter"
   - Remove kimi-k2-thinking and kimi-k2.5 entries entirely

**What NOT to do:**
- Do NOT modify the SyntheticProvider class itself (only remove 3 instances)
- Do NOT touch TOGETHER_PROVIDERS or OPENROUTER_PROVIDERS
- Do NOT rename any remaining -syn model IDs (the 10 Synthetic-exclusive models KEEP their -syn suffix per research findings)
  </action>
  <verify>
Run `npx next build --webpack 2>&1 | tail -30` to verify the application builds without errors. The build will fail if validateProviderRoutes() detects missing providers in routes or if import references are broken. Also verify with `grep -c 'syn' src/lib/llm/providers/synthetic.ts` that -syn still appears in the 10 remaining provider IDs (should be >= 10 occurrences).
  </verify>
  <done>
MODEL_PROVIDER_ROUTES has no -syn entries for the 3 consolidated models. SYNTHETIC_PROVIDERS exports exactly 10 providers. kimi-k2-thinking and kimi-k2.5 entries removed from routes (single-provider, no route needed). Application builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create post-consolidation operational script</name>
  <files>scripts/post-consolidation.ts</files>
  <action>
Create `scripts/post-consolidation.ts` that performs three operations after the database migration has been executed:

**1. Deactivate old -syn model rows in the models table:**
```typescript
// Deactivate the 3 consolidated -syn model rows
// These rows may have been created by sync-models or by the migration's ensureBaseModelRows
const OLD_SYN_IDS = ['deepseek-r1-0528-syn', 'kimi-k2-thinking-syn', 'kimi-k2.5-syn'];
// UPDATE models SET active = false WHERE id IN (OLD_SYN_IDS)
// Log: how many rows deactivated
```

**2. Cache invalidation (comprehensive):**
Use existing patterns from `src/lib/cache/redis.ts`:
```typescript
import { getRedis, cacheDeletePattern, cacheDelete, cacheKeys } from '@/lib/cache/redis';

await Promise.all([
  cacheDelete(cacheKeys.activeModels()),
  cacheDelete(cacheKeys.activeModelCount()),
  cacheDelete(cacheKeys.topPerformingModel()),
  cacheDelete(cacheKeys.allModelHealth()),
  cacheDelete(cacheKeys.overallStats()),
  cacheDeletePattern('db:leaderboard:*'),
  cacheDeletePattern('db:stats:*'),
  cacheDeletePattern('db:model:*:stats'),
  cacheDeletePattern('db:predictions:*'),
  cacheDeletePattern('db:models:*'),
]);
```

**3. Post-migration integrity verification:**
Run these verification queries and output results:
- Count orphaned FKs in predictions (model_id NOT IN models.id) -- expect 0
- Count orphaned FKs in llm_model_stats -- expect 0
- Count orphaned FKs in bets -- expect 0
- Count orphaned FKs in model_balances -- expect 0
- Count orphaned FKs in model_usage -- expect 0
- Count remaining -syn model_id references across all 5 tables for the 3 consolidated IDs -- expect 0
- Count unique constraint violations in predictions (match_id, model_id duplicates) -- expect 0
- Count total active models -- report for comparison
- Print clear PASS/FAIL for each check

**Script structure:**
- Import dotenv/config, getDb, relevant schema tables, cache functions
- Parse --dry-run flag (if set, skip deactivation but run verification)
- Print "=== Post-Consolidation: Phase 63 ===" header
- Run deactivation, cache invalidation, verification in sequence
- Exit 0 on success, exit 1 if any verification fails
- Support --verbose for detailed output

**Important:** This script is designed to run AFTER `migrate-consolidate-models.ts` has been executed successfully. It completes the consolidation by cleaning up the application layer.
  </action>
  <verify>
Run `npx tsx scripts/post-consolidation.ts --dry-run 2>&1 | head -40` to verify the script runs without syntax errors and produces verification output. The --dry-run flag should skip deactivation but still run verification queries. Check that the output includes PASS/FAIL lines for each integrity check.
  </verify>
  <done>
Post-consolidation script exists at scripts/post-consolidation.ts. Script handles model deactivation, comprehensive cache invalidation (all model-keyed caches including leaderboard), and post-migration integrity verification with clear PASS/FAIL output. Supports --dry-run for safe preview.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes (or `npx next build --webpack`) -- validates provider routes at module load
2. SYNTHETIC_PROVIDERS has exactly 10 entries (was 13)
3. MODEL_PROVIDER_ROUTES has no -syn provider IDs
4. kimi-k2-thinking and kimi-k2.5 removed from MODEL_PROVIDER_ROUTES (single-provider entries)
5. scripts/post-consolidation.ts exists and runs with --dry-run
6. No remaining references to 3 consolidated -syn IDs in route configuration
</verification>

<success_criteria>
- Provider configuration reflects 3-model consolidation (39 total models: 29 Together + 10 Synthetic)
- MODEL_PROVIDER_ROUTES contains only multi-provider routes (deepseek-r1 2-tier, qwen3-235b 2-tier, llama-4-scout 2-tier)
- Post-consolidation script ready for production execution
- Application builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/63-model-consolidation-execution/63-01-SUMMARY.md`
</output>
