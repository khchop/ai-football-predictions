---
phase: 27-hero-component
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "MatchHero renders correctly in dev server"
    - "Score displays exactly once (no duplicates)"
    - "All states visually correct: upcoming, live, finished"
  artifacts: []
  key_links: []
---

<objective>
Verify MatchHero component renders correctly across all match states.

Purpose: Ensure the hero component built in Plan 01 displays correctly before integrating into match pages. Visual verification catches styling issues that TypeScript compilation cannot detect.

Output: User-approved hero component ready for Page 28+ integration.
</objective>

<execution_context>
@/Users/pieterbos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieterbos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-hero-component/27-01-SUMMARY.md
@src/components/match/match-hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test harness for MatchHero</name>
  <files>src/app/test-hero/page.tsx</files>
  <action>
Create a temporary test page to visually verify MatchHero component across different match states.

The test page should:
1. Import MatchDataProvider and MatchHero
2. Create mock match data for each state: upcoming, live, finished, postponed, cancelled
3. Render each state in separate sections with labels
4. Be a Server Component that passes mock data to MatchDataProvider

Example structure:
```tsx
import { MatchDataProvider } from '@/components/match/match-data-provider';
import { MatchHero } from '@/components/match/match-hero';

const mockUpcoming = {
  id: 'test-1',
  externalId: null,
  homeTeam: 'Manchester United',
  awayTeam: 'Liverpool',
  homeTeamLogo: '/team-logos/man-utd.png',
  awayTeamLogo: '/team-logos/liverpool.png',
  homeScore: null,
  awayScore: null,
  status: 'scheduled',
  kickoffTime: new Date(Date.now() + 86400000).toISOString(), // Tomorrow
  // ... other required fields
};

const mockLive = {
  ...mockUpcoming,
  id: 'test-2',
  externalId: '12345',
  homeScore: 2,
  awayScore: 1,
  status: 'live',
  kickoffTime: new Date(Date.now() - 3600000).toISOString(), // 1 hour ago
};

const mockFinished = {
  ...mockUpcoming,
  id: 'test-3',
  homeScore: 3,
  awayScore: 0,
  status: 'finished',
  kickoffTime: new Date(Date.now() - 86400000).toISOString(), // Yesterday
};

const mockPostponed = {
  ...mockUpcoming,
  id: 'test-4',
  status: 'postponed',
};

const mockCancelled = {
  ...mockUpcoming,
  id: 'test-5',
  status: 'cancelled',
};

const mockCompetition = {
  id: 'comp-1',
  name: 'Premier League',
  // ... other required fields
};

export default function TestHeroPage() {
  return (
    <div className="container mx-auto py-8 space-y-12">
      <h1 className="text-3xl font-bold">MatchHero Test Harness</h1>

      <section>
        <h2 className="text-xl mb-4">Upcoming Match</h2>
        <MatchDataProvider match={mockUpcoming} competition={mockCompetition} analysis={null}>
          <MatchHero />
        </MatchDataProvider>
      </section>

      <section>
        <h2 className="text-xl mb-4">Live Match</h2>
        <MatchDataProvider match={mockLive} competition={mockCompetition} analysis={null}>
          <MatchHero />
        </MatchDataProvider>
      </section>

      <section>
        <h2 className="text-xl mb-4">Finished Match (Home Win)</h2>
        <MatchDataProvider match={mockFinished} competition={mockCompetition} analysis={null}>
          <MatchHero />
        </MatchDataProvider>
      </section>

      <section>
        <h2 className="text-xl mb-4">Postponed Match</h2>
        <MatchDataProvider match={mockPostponed} competition={mockCompetition} analysis={null}>
          <MatchHero />
        </MatchDataProvider>
      </section>

      <section>
        <h2 className="text-xl mb-4">Cancelled Match</h2>
        <MatchDataProvider match={mockCancelled} competition={mockCompetition} analysis={null}>
          <MatchHero />
        </MatchDataProvider>
      </section>
    </div>
  );
}
```

Include all required Match type fields based on Match interface from @/types or @/lib/db/schema.
  </action>
  <verify>
Dev server runs without errors:
```bash
npm run dev
```
Page loads at http://localhost:3000/test-hero
  </verify>
  <done>Test harness page exists and renders all 5 match states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MatchHero component with all match state variations rendered in test harness</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Navigate to http://localhost:3000/test-hero
3. Verify each section renders correctly:

**Upcoming Match:**
- [ ] Shows "VS" text (large, centered) between teams
- [ ] Shows team logos and full names on left/right
- [ ] Shows "Upcoming" badge
- [ ] Shows kickoff date and time
- [ ] Shows competition name

**Live Match:**
- [ ] Shows score "2 - 1" (large, centered)
- [ ] Shows "LIVE" badge (red)
- [ ] Shows match minute OR time if polling not active
- [ ] NO pulsing/animation on LIVE badge

**Finished Match:**
- [ ] Shows score "3 - 0" (large, centered)
- [ ] Shows "FT" badge
- [ ] Winner (home) highlighted in green (team name and score)
- [ ] Shows kickoff date

**Postponed Match:**
- [ ] Shows "POSTPONED" text instead of score
- [ ] Appropriate badge/styling

**Cancelled Match:**
- [ ] Shows "CANCELLED" text (with line-through per decisions)
- [ ] Appropriate badge/styling

**General:**
- [ ] No venue shown (per user decisions)
- [ ] No form or league position shown
- [ ] Clean/minimal visual design
- [ ] Mobile responsive (shrink browser window)
  </how-to-verify>
  <resume-signal>Type "approved" if hero looks correct, or describe specific issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Clean up test harness</name>
  <files>src/app/test-hero/page.tsx</files>
  <action>
After verification is approved, delete the test harness page:
```bash
rm -rf src/app/test-hero
```

This temporary page is only needed for visual verification - the actual integration happens in Phase 28+.
  </action>
  <verify>Test page no longer exists: `ls src/app/test-hero 2>/dev/null || echo "Cleaned up"`</verify>
  <done>Test harness removed, MatchHero ready for integration.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Test harness was created and used for verification
2. User approved the visual appearance
3. Test harness was cleaned up
4. MatchHero component ready for Phase 28+ integration
</verification>

<success_criteria>
- User has visually verified MatchHero renders correctly
- All 5 match states display appropriately
- No visual issues blocking Phase 28+ integration
- Test harness cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/27-hero-component/27-02-SUMMARY.md`
</output>
