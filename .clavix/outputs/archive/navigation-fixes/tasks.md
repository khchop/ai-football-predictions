# Implementation Plan

**Project**: navigation-fixes
**Generated**: 2026-01-26T00:00:00Z

## Technical Context & Standards
*Detected Stack & Patterns*
- **Architecture**: Next.js 14 App Router
- **Framework**: Next.js 16 (from AGENTS.md)
- **Styling**: Tailwind CSS + shadcn/ui
- **Components**: Custom navigation, Card, DropdownMenu
- **Routing**: `/leagues/[slug]` exists for league pages, `/predictions/[league]` exists for predictions

---

## Issues Identified

1. **Matches link missing from top nav** - `navigation.tsx` only has Home, Leaderboard, Blog
2. **League dropdown links wrong URL** - Links go to `/predictions/{id}` but should go to `/leagues/{id}`
3. **Emojis in navigation** - Used as fallback icons in league display
4. **Home not first in nav** - LeagueSelector appears before navItems in the nav bar
5. **Quick access element clutters matches page** - `<QuickLeagueLinks />` component on `/matches` page

---

## Phase 1: Fix Navigation Structure

- [x] **Add Matches link to top navigation** (ref: Issue 1)
  Task ID: `phase-1-nav-01`
  > **Implementation**: Modify `src/components/navigation.tsx`.
  > **Details**: Add `{ href: '/matches', label: 'Matches', icon: Calendar }` to the `navItems` array. Place it as the second item (after Home).

- [x] **Reorder navigation items** (ref: Issue 4)
  Task ID: `phase-1-nav-02`
  > **Implementation**: Modify `src/components/navigation.tsx`.
  > **Details**: Move `<LeagueSelector />` component to appear AFTER the `{navItems.map(...)` loop. This ensures Home, Matches, Leaderboard, Blog appear as the first nav items, then LeagueSelector.

## Phase 2: Fix League Dropdown

- [x] **Change league links to correct URL** (ref: Issue 2)
  Task ID: `phase-2-league-01`
  > **Implementation**: Modify `src/components/league-selector.tsx`.
  > **Details**: Change all links from `/predictions/${competition.id}` to `/leagues/${competition.id}`. Update `isActive` checks from `pathname === \`/predictions/${competition.id}\`` to `pathname === \`/leagues/${competition.id}\``.

- [x] **Remove emoji fallback from league display** (ref: Issue 3)
  Task ID: `phase-2-league-02`
  > **Implementation**: Modify `src/components/league-selector.tsx`.
  > **Details**:
  > 1. Line 48: Change `{competition.icon || '⚽'}` to just `{competition.icon}` (remove emoji fallback)
  > 2. Line 80: Change `{currentLeague.icon || ''} ${currentLeague.name}` to just `{currentLeague.name}` (remove icon prefix)

## Phase 3: Clean Up Matches Page

- [x] **Remove QuickLeagueLinks from matches page** (ref: Issue 5)
  Task ID: `phase-3-matches-01`
  > **Implementation**: Modify `src/app/matches/page.tsx`.
  > **Details**:
  > 1. Remove the import: `import { QuickLeagueLinks } from '@/components/quick-league-links';`
  > 2. Remove line 301: `<QuickLeagueLinks />`

---

*Generated by Clavix /clavix-plan*
