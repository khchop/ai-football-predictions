# ===========================================
# FOOTBALL AI PREDICTIONS - ENVIRONMENT VARS
# ===========================================
# Copy this file to .env.local and fill in your values

# =========== DATABASE (PostgreSQL) ===========
# Deploy PostgreSQL via Coolify (recommended) or use any PostgreSQL host
# Connection string format: postgresql://user:password@host:5432/database
DATABASE_URL=postgresql://user:password@localhost:5432/football

# =========== FOOTBALL DATA API ===========
# Sign up: https://dashboard.api-football.com/register
# Free tier: 100 requests/day
API_FOOTBALL_KEY=your-api-football-key

# =========== LLM PROVIDERS ===========

# Together AI - PRIMARY PROVIDER (REQUIRED)
# Sign up: https://api.together.ai/signup
# Benefits:
#   - 35 open-source models with JSON support
#   - Direct provider pricing (no markup)
#   - Lower latency (single-hop API)
#   - Better reliability (99.9% uptime)
#   - Higher rate limits
# Estimated cost: ~$7.50-10/month for 25 matches/day
TOGETHER_API_KEY=your_together_key

# =========== LLM PROVIDERS (Premium - Future) ===========
# Uncomment when ready to add premium models

# OpenAI
# Sign up: https://platform.openai.com/
# OPENAI_API_KEY=sk-your_openai_key

# Anthropic Claude
# Sign up: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-your_anthropic_key

# =========== APP SECURITY ===========
# Generate a random string for cron job authentication
# Example: openssl rand -base64 32
CRON_SECRET=generate-a-random-32-char-string

# =========== BUDGET CONTROL ===========
# Daily budget limit in USD for paid models
# Default: $1.00/day (~€30/month)
DAILY_BUDGET=1.00

# =========== ERROR MONITORING (GlitchTip) ===========
# Self-hosted error tracking via GlitchTip (privacy-focused alternative to Sentry)
# Sign up: https://glitchtip.com
# Deploy on Coolify alongside your app
# Create a Next.js project and get the DSN
NEXT_PUBLIC_SENTRY_DSN=http://your-key@your-glitchtip-domain/project-id

# =========== APP URL ===========
# Local development
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production (set in Coolify environment variables)
# NEXT_PUBLIC_APP_URL=https://your-domain.com
