// Betting system constants

export const BETTING_CONSTANTS = {
  // Starting balance for each model
  STARTING_BALANCE: 1000.00,
  
  // Fixed stake per bet
  STAKE_PER_BET: 1.00,
  
  // Number of bets per match
  BETS_PER_MATCH: 3,
} as const;
